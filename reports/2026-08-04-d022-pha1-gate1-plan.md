# Gate 1 — kế hoạch Pha 1 E08-D022 (port UI R0 → `/crm`)

> Báo cáo PM con gửi PM tổng. Chưa code. **Có một đề nghị đổi *hướng* port** — cần PMt chốt trước.
> **Ngày:** 2026-08-04 · **Base:** `1f302fc` · prod `CRM_UI=new` (chỉ đọc, không đổi)

---

## 1. Thực trạng đo được

`appShell()` ([`index.js:16`](../server/crm/index.js)) với `CRM_UI=new` phục vụ **`crm-app-v2.html`**. R0 sửa **`crm-app.html`** (shell classic) — chỉ có ở `/crm/classic`. Nên **toàn bộ nâng cấp của Ly hiện không hiện trên `/crm`**.

Hai file **không phải bản cũ/mới của nhau nữa** — chúng đã rẽ nhánh cấu trúc (id DOM, hàm render, tầng dịch đều khác).

## 2. So sánh tính năng (đo bằng grep, không phỏng đoán)

| Tính năng | `crm-app.html` (R0/Ly) | `crm-app-v2.html` (prod) |
|---|---|---|
| Avatar ảnh `photo_url` (D021) | ✅ có | ✅ có |
| Tab buổi Tọa đàm/Gala (D020) | ✅ có (lọc **client** theo tag) | ✅ có (lọc qua **API** `session=`) |
| Check-in POST · RBAC `btl` · 401 fail-closed · `limit=1000/100` | ✅ **giống hệt** | ✅ |
| **KPI D016 `/crm/stats`** | ❌ **không có** | ✅ có |
| i18n Việt + **Nhật** (`T={vn,jp}`) | ✅ | ❌ |
| 2 đếm ngược + 2 địa điểm riêng, sửa được | ✅ | ❌ |
| Ẩn SĐT `maskPhone` (`xxxxxxx`+3 số) | ✅ | ❌ |
| Cờ ⚠ TRÙNG tên | ✅ | ❌ |
| Xuất CSV **+ Excel** | ✅ | một phần (CSV) |
| Cài đặt sự kiện (localStorage) · sắp xếp VIP/bàn/trạng thái | ✅ | ❌ |

API hai bên dùng **trùng nhau hoàn toàn**, khác đúng một endpoint: v2 gọi thêm `/crm/stats`.

## 3. 🔴 Đề nghị đổi hướng port (giữ nguyên mục tiêu Path A)

Phiếu ghi: *port công UI Ly → `crm-app-v2.html`, giữ `CRM_UI=new`, giữ KPI, không lật classic*. Bốn ràng buộc đó **em giữ hết**. Chỉ xin đổi **chiều chép**:

| | Theo chữ trong phiếu | Đề nghị |
|---|---|---|
| Việc phải làm | Chép **~8 vùng tính năng** của Ly vào bộ khung v2 | Chép **1 tính năng** (KPI D016) vào bộ khung Ly |
| Kết quả file `crm-app-v2.html` | Khung v2 + tính năng Ly viết lại theo idiom v2 | Nội dung Ly + khối KPI |
| `/crm` (`CRM_UI=new`) phục vụ | v2 | **v2** — không đổi |
| KPI D016 | giữ | giữ |
| `crm-app.html` | — | giữ nguyên làm đường lùi `/crm/classic` |

**Vì sao:** viết lại i18n vn/jp, 2 đếm ngược có địa điểm sửa được, maskPhone, cờ TRÙNG, export Excel, cài đặt sự kiện — sang một bộ khung khác id/khác hàm render — là **viết lại phần lớn UI của Ly**, 4 ngày trước lễ, trên màn hình Lễ tân/PG đang dùng thật. Chiều ngược lại là **chép một panel KPI** đã qua Gate 2 ba vòng.

Rủi ro đảo hẳn: hướng phiếu có ~8 cơ hội làm hỏng công của Ly; hướng đề nghị có 1 cơ hội làm hỏng KPI — và KPI có sẵn bộ test.

**Kết quả người dùng nhìn thấy là như nhau.** Khác biệt duy nhất là bao nhiêu dòng phải viết lại.

## 4. Kế hoạch thực thi (sau khi PMt chốt chiều)

### Nếu chốt chiều đề nghị

1. `crm-app-v2.html` ← nội dung `crm-app.html` (bản `1f302fc`).
2. Ghép khối KPI D016 vào: CSS `.kpi/.kgrid/.kbox/.ksec/.kpart/.kp/.ksum/.kwarn` · HTML `#kpiCard` · JS `loadStats()` + `renderKpi()` · gọi `loadStats()` sau `/crm/me`.
3. Giữ nguyên 4 chuỗi chú thích AC-7 (Gate 2 grep nguyên văn).
4. Không đụng `server/crm/*` backend ngoài mục 5.

### Nếu giữ đúng chữ trong phiếu

Port lần lượt 8 vùng ở §2, mỗi vùng một commit, smoke sau từng vùng. Em ước lượng **rủi ro cao hơn đáng kể** và xin ghi rõ điều đó vào log.

### Chung cho cả hai

5. **`GET /crm/guests` thêm `note`** (additive, 1 dòng SELECT — [`guests.js:43`](../server/crm/guests.js)). Đã verify: list **chưa** có `note`, detail `:id` đã có. FE Ly dùng `noteF(g,"S2 phụ trách")` cho cờ phụ trách → **thiếu `note` thì cột này rỗng**.
6. Gate 2 QC độc lập (§B7, khác actor) → deploy `railway up --service esuhai-web` (luật CR-25) → smoke BTL trên `/crm`.

## 5. Rủi ro phải canh ở Gate 2

| Rủi ro | Cách bắt |
|---|---|
| Mất KPI D016 | `/crm/stats` 401 no-auth · `#kpiCard` hiện · 154/107/29/18 · 4 chuỗi AC-7 grep nguyên văn |
| Mất avatar D021 | list có `photo_url`, 41/154 thẻ có ảnh |
| Hỏng check-in D020 | điểm danh 1 khách thật, counter đổi |
| Rơi RBAC | staff không thấy nút Import/Xoá; `DELETE` → 403 |
| Rơi fail-closed | no-auth `/crm/guests` → 401 |
| Lộ PII | trang không đăng nhập: 0 tên khách |

## 6. Chưa làm — Pha 2 (data)

`data/inbox/` **chưa có** SoT 03–04/08 (chỉ 3 file cũ 28–29/07). Connector SharePoint/ms365 phiên này **chưa được cấp quyền** và phiên không tương tác nên em không tự tải được. Cần anh/Ly đặt file vào `data/inbox/` rồi báo tên. **Không đoán, không ghi prod data.**

## 7. Xin PMt chốt

1. **Chiều port** — theo đề nghị §3, hay giữ đúng chữ phiếu?
2. Xác nhận `note` vào list là additive được duyệt.
