# Chấm lại AC-B0 sau bản vá — E08-D028 (QC ĐỘC LẬP · §B7)

> **Tip chấm:** `c104ae9` · đo trên **prod** `https://esuhai-web-production.up.railway.app` · 2026-08-04
> **Chuẩn đối chiếu:** `reports/2026-08-04-d028-wave-ab0-gate2.md` (vòng trước, verdict FAIL vì L1).
> **Phạm vi hẹp:** (1) hồi quy layout mobile · (2) L6 làn rollback · (3) spot-check L3 + L4 · (4) regress nhanh. **Không** đo lại toàn AC-A.
> QC chỉ Read + Grep + Bash read-only. Mọi SQL bọc `BEGIN TRANSACTION READ ONLY` + `ROLLBACK`. Không sửa file nào ngoài chính báo cáo này, không commit, không deploy, không ghi DB. Token giữ trong file `chmod 600`, đã xoá sau khi chạy. Báo cáo chỉ chứa **số đếm và id nội bộ** — không tên, SĐT, email khách.

---

## VERDICT: **FAIL**

| Mục chấm | Kết quả |
|---|---|
| **1 · B0 hồi quy layout mobile** | ✅ **PASS** — vá đúng và vá sạch. 0 tràn ở **cả 20 bề rộng 280→1440px, cả hai shell**, cả tiếng Nhật, cả chữ phóng to, cả xoay ngang. Chứng minh nhân-quả bằng A/B. Desktop không đổi. |
| **2 · L6 làn rollback** | ⚠️ **PASS một nửa** — 2 link có đủ, đúng URL, `target="_blank"`, `rel="noopener"`, i18n vn/jp, bấm mở được 350 thẻ, không tràn. **Nhưng** `/crm/classic` vẫn đếm buổi theo tag: tab hiện **149 / 303** trong khi `/crm` hiện **156 / 350** → **lỗi mới N2**. |
| **3a · L4 (regex biên từ)** | ✅ **PASS** — 26 chuỗi thử trên Postgres, **0 chuỗi rác khớp nhầm**. `Salad` · `Balalaika` · `Galaxy` · `Bengala` · `Galas` · `MASALA` đều false. |
| **3b · L3 (nhãn «Buổi»)** | ❌ **FAIL** — file Xuất **đã đúng**, nhưng **hồ sơ khách hỏng nặng hơn trước khi vá**: nay **cả 376/376** khách đều hiện «BUỔI: —», kể cả 305 khách có tag mà trước bản vá hiện đúng. **Lỗi mới N1 — hồi quy do chính `f9c7b1d` gây ra.** |
| **4 · Regress nhanh** | ✅ **PASS** — `tab \| ?session= \| KPI` lệch **0** ở cả hai lần chụp, khớp tới **từng ID** với SQL độc lập. 400/401/403 đúng. `npm run smoke:crm` **43/43**. |

**Chấm FAIL vì mục 3b.** Bản vá `f9c7b1d` tự giới thiệu là sửa L3, nhưng nó **đổi hồ sơ khách từ đúng-cho-305-người thành sai-cho-cả-376-người**. Đây không phải lỗi cũ còn sót — là hồi quy mới, sinh ra bởi đúng commit đang được chấm, trên đúng màn PG mở ở cửa.

**Phần B0 chính thì đã sạch.** Nếu PMt tách vé, AC-B0 (layout + 2 link) **đạt**; hai lỗi chặn nằm ở L3 và ở làn rollback.

---

## 0 · Prod đang chạy đúng bản được chấm

```
$ git rev-parse --short HEAD        → c104ae9
$ diff <(git show c104ae9:<file>) <(curl prod)
IDENTICAL  prod ↔ c104ae9:server/crm/views/crm-app-v2.html
IDENTICAL  prod ↔ c104ae9:server/crm/views/crm-app.html
IDENTICAL  prod ↔ c104ae9:checkin-gala.html
IDENTICAL  prod ↔ c104ae9:checkin-toadam.html
```

⚠️ **Cây làm việc đang bẩn trong lúc QC chạy** — worker đang viết tiếp AC-B2/B3:

```
$ git status --short
 M checkin-gala.html      M checkin-toadam.html      M server/crm/views/crm-app-v2.html
```

Các sửa đổi đó (`ci_at_door`, `.ci-off`, banner `#bnTot`, ẩn nút điểm danh với `staff`) **chưa commit, chưa lên prod**. Toàn bộ kết luận dưới đây thuộc về **`c104ae9`**, đo trên bản đang phục vụ thật. Nếu chấm bằng `diff` với working tree thì sẽ báo «DIFFERS» sai — QC đã đối chiếu với **blob của commit**, không với file trên đĩa.

⚠️ **Dữ liệu prod trôi trong lúc QC chạy** (hệ thống đang nhận đăng ký thật): đầu phiên `crm_guests` sống = **376**, cuối phiên = **378** (`created_at > now()-90min` = **2**). Vì vậy bảng số ở mục 4 có **hai lần chụp**; bất biến 0-lệch đúng ở **cả hai** — đó là bằng chứng mạnh hơn một lần chụp, vì luật vẫn giữ khi dữ liệu đổi.

**Role đo được:** `/crm/me` → `staff`. `SELECT role, count(*) FROM staff_users GROUP BY 1` → **`btl: 4`**, vẫn **không có dòng `staff`** nào. Kết luận về `btl` vẫn chỉ theo cấu tạo, như vòng trước.

---

## Mục 1 — B0 hồi quy layout mobile: **vá đúng**

Đo bằng Chromium thật (Playwright, header Bearer, **abort mọi request rời origin** để token không đi ra ngoài). Không dùng lại số của worker.

### 1.1 Bảng đo từng viewport (số thật, đơn vị px)

`/crm` (crm-app-v2.html):

| vp | `doc.scrollWidth` / `innerWidth` | `.top-in` `scrollWidth`/`clientWidth` | tràn | `#langBtn` right | `#themeBtn` right | `#menu` right | `.cknav a` sw/cw | cắt chữ |
|---|---|---|---|---|---|---|---|---|
| **390** | 390 / 390 | 390 / 390 | **0** | 294 ✅ | 335 ✅ | **376 ✅** | 176/176 · 176/176 | **0** |
| **360** | 360 / 360 | 360 / 360 | **0** | 264 ✅ | 305 ✅ | **346 ✅** | 161/161 · 161/161 | **0** |
| **320** | 320 / 320 | 320 / 320 | **0** | 224 ✅ | 265 ✅ | **306 ✅** | 141/141 · 141/141 | **0** |
| **1200** | 1200 / 1200 | 1080 / 1080 | **0** | 1044 ✅ | 1085 ✅ | 1126 ✅ | 99/99 · 76/76 | **0** |

`/crm/classic` (crm-app.html): **số đo trùng khít từng px** với bảng trên ở cả 4 viewport.

- Vòng trước ở 390px: `#langBtn` 436→480, `#themeBtn` 487→512, `#menu` 519→553 — **cả ba nằm ngoài màn 390**, và «Cửa Gala» bị cắt 36px.
- Nay: **cả ba `right` ≤ `innerWidth`** ở mọi viewport. `scrollWidth === clientWidth` ở mọi viewport. `.cknav a` **`scrollWidth === clientWidth`** → **hiện đủ chữ**, không ellipsis, không cắt.

### 1.2 Desktop 1200px không vỡ

`getComputedStyle(.top-in).flex-wrap` = **`nowrap`** ở 1200px (media query `max-width:640px` không chạm tới). Nav nằm **cùng hàng** với brand:

```
.brand  top=14        .cknav  top=14   (BẰNG NHAU → cùng hàng)
.cknav  left=347 right=532        .top-r  left=986 right=1126
.top    height = 120px  (mobile 230px)   .top-in height = 54px
```

Ảnh chụp 1200px xác nhận bằng mắt: logo · ESUHAI · ngũ hành · STAFF · «Cửa Tọa đàm» «Cửa Gala» — rồi 日本語 · ☾ · ⋯ dồn phải. **Giống hệt bố cục cũ.**

### 1.3 A/B chứng minh nhân-quả (chỉ trong trình duyệt QC, không đụng server)

Tắt **đúng bản vá** bằng `add_style_tag(".top-in{flex-wrap:nowrap!important}.cknav{flex-basis:auto!important;order:0!important}")`:

| vp | **AFTER** (vá đang chạy) | **BEFORE** (ép lại `nowrap`) | **NONAV** (gỡ hẳn `.cknav`, ≈ trước D028) |
|---|---|---|---|
| **390** | doc **390** · lang/theme/menu **in-view** · cắt 0 | doc **565** · **cả 3 OFFSCREEN** · «Cửa Gala» **OFFSCREEN** | doc **390** · in-view |
| **360** | doc **360** · in-view · cắt 0 | doc **565** · **cả 3 OFFSCREEN** | doc **370** · **`#menu` OFFSCREEN** |
| **320** | doc **320** · in-view · cắt 0 | doc **565** · **cả 3 OFFSCREEN** + cả 2 link OFFSCREEN | doc **370** · **`#themeBtn` + `#menu` OFFSCREEN** |

Hai điều đọc ra từ bảng này:

1. **Bản vá là nguyên nhân duy nhất của việc hết tràn.** Chỉ cần ép `flex-wrap` về `nowrap` là lỗi cũ quay lại nguyên vẹn ở cả ba viewport.
2. **Bản vá còn tốt hơn trạng thái trước D028.** Cột NONAV cho thấy ở 360/320 thì `#menu` (chứa **Đăng xuất**) **đã văng khỏi màn từ trước khi có D028** — `.top-in` vốn `nowrap` nên header vẫn tràn 10px/50px. `flex-wrap:wrap` sửa luôn cả lỗi cũ đó.

*(Ghi chú trung thực: BEFORE của tôi ra **565** chứ không phải **553** như vòng trước, vì tôi chỉ ép lại `flex-wrap`/`order`/`flex-basis` mà `.cknav a{padding:4px 10px;font-size:.7rem}` mới vẫn còn áp; luật cũ là `@media(max-width:520px){padding:4px 8px;font-size:.68rem}`. Chênh 12px là do khác cỡ chữ nút, không đổi kết luận.)*

### 1.4 Quét biên media query — 20 bề rộng × 2 shell = 40 phép đo, **0 vỡ**

`280 · 320 · 360 · 375 · 390 · 414 · 430 · 480 · 540 · 600 · 639 · 640 · 641 · 700 · 768 · 820 · 900 · 1024 · 1200 · 1440`

```
BAD (tràn / cắt chữ / nút ngoài màn): none
```

Chuyển pha ở đúng ranh: `640 → wrap` (nav xuống hàng riêng), `641 → nowrap` (nav cùng hàng). Không có khoảng rộng nào «kẹt giữa» hai luật mà vỡ.

`.top` height theo bề rộng: `280→265` · `320..430→230` · `480..640→158` · `≥641→120`.

---

## Mục 2 — L6 làn rollback `/crm/classic`

Đo trên trình duyệt thật, `/crm/classic` (route `index.js:30` luôn phục vụ shell cũ bất kể `CRM_UI`):

```json
[{"href":"/checkin-toadam.html","target":"_blank","rel":"noopener","i":"nav_toa","text":"Cửa Tọa đàm"},
 {"href":"/checkin-gala.html",  "target":"_blank","rel":"noopener","i":"nav_gala","text":"Cửa Gala"}]
```

| Kiểm | Kết quả |
|---|---|
| Đủ 2 link, đúng URL | ✅ |
| `target="_blank"` + `rel="noopener"` | ✅ cả hai |
| i18n vn/jp | ✅ `nav_toa` «Cửa Tọa đàm»/«受付 座談会» · `nav_gala` «Cửa Gala»/«受付 ガラ» (đo sau khi bấm `#langBtn`) |
| Không tràn ở 390/360/320 | ✅ số đo **trùng khít** bảng 1.1 |
| Bấm thật → mở được | ✅ tab mới `/checkin-gala.html`, **350 thẻ**, `#cTot` = `350`, banner «350 khách · 0 đã đến» |
| Lỗi JS | **0** |

❌ **Nhưng xem lỗi mới N2 dưới đây** — link thì có, còn danh sách bên trong `/crm/classic` vẫn là bản trước D028.

---

## Mục 3 — Spot-check L3 + L4

### 3a · L4 — regex biên từ: **PASS**

Chạy **chính hằng `SESSION_RE` trong `guests.js:23-26`** trên Postgres (`SELECT … FROM (VALUES …)`, `BEGIN TRANSACTION READ ONLY`), toán tử `~*` đúng như code:

```
'toa-dam': (^|[^[:alpha:]])(t[oọ]a[ ]?đ[aà]m|toa dam)([^[:alpha:]]|$)
'gala'   : (^|[^[:alpha:]])gala([^[:alpha:]]|$)
```

8 chuỗi vé yêu cầu:

| `sessions` | toa MỚI | gala MỚI | gala CŨ `%ala%` | đúng? |
|---|---|---|---|---|
| `Gala` | false | **true** | true | ✅ |
| `Tọa đàm · Gala` | **true** | **true** | true | ✅ |
| `Salad` | false | **false** | **true** ← lỗi cũ | ✅ **đã chặn** |
| `Balalaika` | false | **false** | **true** ← lỗi cũ | ✅ **đã chặn** |
| `GALA` | false | **true** | true | ✅ (`~*` không phân biệt hoa/thường) |
| `gala tối` | false | **true** | true | ✅ |
| `Toa dam` | **true** | false | false | ✅ (luật cũ `%ọa đàm%` **bỏ sót** ca này) |
| `Tọa đàm` | **true** | false | false | ✅ |

QC tự thêm 18 ca hiểm, đáng chú ý:

| `sessions` | toa | gala | nhận xét |
|---|---|---|---|
| `Galaxy` · `Bengala` · `Galas` · `MASALA` | false | **false** | ✅ biên từ chặn cả tiếp đầu lẫn tiếp đuôi |
| `Gala-dinner` · `gala2` · `La gala đó` · `ĐÀM GALA` | false | true | ✅ dấu/số/khoảng trắng đều là biên hợp lệ |
| `Gala·Tọa đàm` (không dấu cách) | true | true | ✅ |
| `TỌA ĐÀM · GALA` | true | true | ✅ |
| `Toa  dam` (hai dấu cách) | **false** | false | ⚠️ `[ ]?` chỉ cho **một** dấu cách — xem N4 |
| `Tòa đàm` (sai dấu `òa`/`ọa`) | **false** | false | ⚠️ pre-existing, xem N4 |
| `normalize('Tọa đàm · Gala', NFD)` | **false** | true | ⚠️ **L5 chưa vá** — xem N5 |
| `toa-dam` (slug) | **false** | false | ⚠️ lệch `stats.js` — xem N3 |

```
chuỗi RÁC bị khớp nhầm (mong đợi 0):  0
```

**Trên dữ liệu thật, luật mới và luật cũ cho kết quả y hệt** — không ai đổi phe, nên đổi regex **không xê dịch một khách nào** hôm nay:

```sql
SELECT count(*) FILTER (WHERE (ss ILIKE '%ala%')     <> (ss ~* RE_GALA)) AS gala_doi_phe,
       count(*) FILTER (WHERE (ss ILIKE '%ọa đàm%')  <> (ss ~* RE_TOA))  AS toa_doi_phe
→ gala_doi_phe 0 · toa_doi_phe 0   (trên 47 khách có response_id)
```

Bảng `rsvp_submissions` hôm nay: `Gala` ×36 · `Tọa đàm · Gala` ×8 · `NULL` ×1 — **tất cả NFC**.

### 3b · L3 — nhãn «Buổi»: **FAIL, và tệ hơn trước khi vá**

`buoiOf()` **đã** đổi sang `du_*` (`crm-app-v2.html:352`) đúng như worker mô tả. Nhưng nó được dùng ở **hai đường có nguồn dữ liệu khác nhau**, và worker chỉ kiểm một:

| Đường | Nguồn dữ liệu | `du_*` có không? | Kết quả |
|---|---|---|---|
| **File Xuất CSV/Excel** (`:555 guestRows()`) | mảng `ALL` ← `GET /crm/guests` | ✅ có | ✅ **ĐÃ SỬA** |
| **Hồ sơ khách** (`:532` trong `renderGuest`) | `j.guest` ← **`GET /crm/guests/:id`** | ❌ **KHÔNG có** | ❌ **HỎNG** |

**Xuất CSV — bấm nút thật, tải file thật, đọc cột «Buổi» (376 dòng):**

```
header: Họ tên · Đơn vị · Chức danh · Buổi · Hạng VIP · Bàn · SĐT (ẩn) · Trạng thái · Giờ đến · Phụ trách
phân bố cột Buổi:  "Gala Lễ kỷ niệm" 196 · "Tọa đàm chuyên đề + Gala Lễ kỷ niệm" 154
                   "Tọa đàm chuyên đề" 2 · "—" 24
→ Gala 196+154 = 350 ✓   Tọa đàm 154+2 = 156 ✓   "—" = 24 (đúng nhóm sessionUnknown)
```

✅ **File xuất cho BTL nay ghi đúng buổi.** L3 phần này xong.

**Hồ sơ khách — bấm thẻ thật trên UI, đọc ô nhãn (không đọc tên/SĐT):**

```json
{"guest_id":"106","tags có ',gala,'":true,       "o_Buoi_tren_ho_so":"—"}
{"guest_id":"248","tags có ',toa-dam,gala,'":true,"o_Buoi_tren_ho_so":"—"}
{"guest_id":"175","vào Gala nhờ form":true,       "o_Buoi_tren_ho_so":"—"}
```

Ảnh chụp vùng `.facts` của id 248 (khách có **cả hai** tag): ô hiện đúng hai ký tự **`BUỔI —`**.

**Nguyên nhân, đo được chứ không suy luận** — payload của endpoint hồ sơ:

```
$ GET /crm/guests/175
keys_of_guest = [created_at, email, full_name, id, note, org, phone,
                 response_id, table_no, tags, title]
has_du_gala = false   has_du_toa_dam = false
```

Lấy mẫu **16 khách** rải đều toàn danh sách: **16/16** thiếu cả hai cột `du_*`. Chạy hai luật trên cùng payload đó:

```
buoiOf CŨ  (theo tag , có trong payload) ra nhãn thật:  15/16
buoiOf MỚI (theo du_*, KHÔNG có payload) ra nhãn thật:   0/16
```

**→ Đây là hồi quy, không phải lỗi còn sót.** Trước `f9c7b1d`, hồ sơ hiện đúng buổi cho **305/376** khách có tag và «—» cho 71 người. Sau `f9c7b1d`, hồ sơ hiện «—» cho **376/376**. Bản vá làm hỏng thêm **305 hồ sơ** để cứu 47 hồ sơ — mà 47 hồ sơ đó **cũng chưa được cứu**.

---

## Mục 4 — Regress nhanh

### 4.1 `tab | ?session= | KPI`

Prod nhận đăng ký thật trong lúc QC chạy, nên có **hai lần chụp**. Mỗi lần gom 4 nguồn trong **418 ms** (`Promise.all`) để không bị lệch do thời gian.

**Chụp 1** (`crm_guests` sống = 376):

| Buổi | tab (`du_*` server trả) | `?session=` | KPI `/crm/stats` | KPI hiện trên màn | **Lệch** |
|---|---|---|---|---|---|
| **Gala** | 350 | 350 | 350 | 350 | **0** |
| **Tọa đàm** | 156 | 156 | 156 | 156 | **0** |

**Chụp 2** (~15 phút sau, `crm_guests` sống = 378, `created_at > now()-90min` = **2**):

| Buổi | tab | `?session=` | KPI | **Lệch** |
|---|---|---|---|---|
| **Gala** | 352 | 352 | 352 | **0** |
| **Tọa đàm** | 156 | 156 | 156 | **0** |

Số trên **màn thật** (Playwright đọc DOM, chụp 1): `Tất cả 376 · Tọa đàm 156 · Gala 350`, và số **thẻ vẽ ra** đúng bằng đó (`all 376 · toa 156 · gal 350`). Khối KPI: «Dự **Gala 350** = 303 từ danh sách + 47 từ đăng ký · Dự **Tọa đàm 156** = 149 + 7».

**Chưa dừng ở tổng — so tới từng ID** với SQL độc lập QC tự viết (luật **khác** worker: tag khớp bằng `= ANY(string_to_array(...))`, form khớp bằng regex `[^[:alnum:]]` chứ không phải `[^[:alpha:]]`):

```
?session=gala    : api=350 sql=350 | chỉ-API=0 chỉ-SQL=0 → SET BẰNG NHAU
?session=toa-dam : api=156 sql=156 | chỉ-API=0 chỉ-SQL=0 → SET BẰNG NHAU
du_gala (tab)    : api=350 sql=350 | chỉ-API=0 chỉ-SQL=0 → SET BẰNG NHAU
du_toa_dam (tab) : api=156 sql=156 | chỉ-API=0 chỉ-SQL=0 → SET BẰNG NHAU
```

Chụp 2 cũng khớp: `QC SQL độc lập → union_gala 352 · union_toa 156`. **`response_id` mồ côi = 0.**

Sáu lớp lệch tiềm ẩn đo trên prod: `lop1_vừa_tag_vừa_form 0` · `lop2_tag_toa_form_gala 0` · `lop3_%ala%_không_phải_gala 0` · `lop4_không_NFC 0` · `lop5_toadam_không_dấu 0`. *(`lop6_form_rỗng 329` là **đúng như mong đợi**, không phải lỗi: 378−49 khách không có `response_id` nên `LEFT JOIN` trả NULL.)*

### 4.2 Mã lỗi

| Phép kiểm | Kết quả |
|---|---|
| `GET /crm/guests?session=xxx` | ✅ **400** `{"ok":false,"error":"session không hợp lệ (toa-dam\|gala)."}` |
| `GET /crm/guests?session=gala-vip` (rò token buổi) | ✅ **400** — whitelist chặn trước khi chạm SQL |
| `GET /crm/guests?session=GALA` (hoa) | ✅ **400** — whitelist phân biệt hoa/thường, không lọt |
| `GET /crm/guests?limit=5` **không auth** | ✅ **401** `{"ok":false,"error":"unauthorized"}` |
| `GET /crm/stats` **không auth** | ✅ **401** |
| `DELETE /crm/guests/999999999` (staff) | ✅ **403** `{"ok":false,"error":"forbidden"}` — chặn ở middleware, **không chạm DB** |

### 4.3 `npm run smoke:crm` — dòng cuối nguyên văn

Chạy chế độ đầy đủ (có `DATABASE_URL` → bật nhánh đối chiếu SQL; có `CRM_SMOKE_BEARER` → bật phần UI Playwright).

```
PASS — 43/43 phép kiểm · https://esuhai-web-production.up.railway.app
```

Vài dòng đáng chú ý: `ok AC-4 /crm/stats khớp SQL độc lập ±0 — SQL 376/305/47/24 vs API 376/305/47/24` · `ok AC-5 không lỗi JS — sạch` · `ok AC-1 /crm/classic không auth = trang login`.

---

# Lỗi còn lại

## N1 — **BLOCKER · HỒI QUY MỚI** · hồ sơ khách nay ghi «BUỔI: —» cho **cả 376 người**

**File:** `server/crm/views/crm-app-v2.html:352` (`buoiOf`) dùng ở **`:532`** (ô «Buổi» trong hồ sơ) ↔ **`server/crm/guests.js:99`** (`GET /crm/guests/:id`)

```js
// crm-app-v2.html:352 — bản vá f9c7b1d
function buoiOf(g){var t=!!g.du_toa_dam,ga=!!g.du_gala; …}

// crm-app-v2.html:519 — hồ sơ lấy dữ liệu từ ENDPOINT KHÁC, và endpoint đó
//                        KHÔNG tính du_toa_dam / du_gala
async function openGuest(id){try{var j=await jget("/crm/guests/"+id);curId=id;renderGuest(j); …}
```

Hai cột `du_toa_dam` / `du_gala` chỉ được tính trong câu `SELECT` của **danh sách** (`guests.js:62-70`). Câu của **hồ sơ** (`guests.js:99`) không có. `renderGuest(j)` đưa `j.guest` — không có `du_*` — vào `buoiOf`, cả hai biến thành `undefined` → `!!undefined` = `false` → luôn rơi vào nhánh `"—"`.

**Trước bản vá tệ hơn hay tốt hơn?** — **tốt hơn.** `tags` **có** trong payload hồ sơ (đo được: id 248 → `"vnjb,toa-dam,gala,pl:ptgd"`), nên `buoiOf` cũ đọc tag vẫn ra nhãn đúng cho 305 người. Bảng đối chiếu trên cùng 16 payload thật: **cũ 15/16 ra nhãn · mới 0/16**.

| | hồ sơ hiện đúng buổi | hồ sơ hiện «—» |
|---|---|---|
| **trước `f9c7b1d`** | 305 | 71 |
| **sau `f9c7b1d` (đang LIVE)** | **0** | **376** |

**Vì sao là blocker:** ngày 08/08 người ở cửa mở hồ sơ một khách đang đứng trong tab Gala, đọc được «chưa rõ buổi» — với **mọi** khách, kể cả khách có tag rõ ràng. Trước vé D028, ít nhất 305 người còn đúng.

**Cách sửa gợi ý (QC KHÔNG tự sửa) — chọn một:**

- **(a) Sửa ở máy chủ, đúng tinh thần «một SoT»:** thêm hai biểu thức `du_toa_dam` / `du_gala` (copy y nguyên từ `guests.js:63-70`) vào câu `SELECT` của `GET /crm/guests/:id` ở `guests.js:99`. Một lần sửa, mọi màn dùng hồ sơ đều đúng.
- **(b) Sửa ở client, một dòng:** trong `renderGuest`, lấy cờ từ dòng đã có trong `ALL` thay vì từ payload hồ sơ —
  `var row=ALL.filter(function(x){return String(x.id)===String(g.id);})[0]||g;` rồi `buoiOf(row)`.
  Rẻ hơn nhưng hồ sơ mở từ đường không qua danh sách sẽ lại «—».
- Cách nào cũng phải **đo lại trên UI thật**: mở hồ sơ id `248` (tag cả hai), `106` (tag gala), `175` (chỉ form) — phải ra «Tọa đàm + Gala», «Gala», «Gala».

## N2 — **MEDIUM** · làn rollback `/crm/classic` có link nhưng danh sách vẫn là bản trước D028

**File:** `server/crm/views/crm-app.html:311-312, 323, 340-341, 378, 401`

```
$ grep -c "du_gala\|du_toa_dam" server/crm/views/crm-app.html   → 0
311: function hasTag(g,x){return tagList(g).indexOf(x)>=0;}
312: function buoiOf(g){var t=hasTag(g,"toa-dam"),ga=hasTag(g,"gala"); …}
323: function tabCount(t){… t==="toa"?hasTag(g,"toa-dam"):t==="gal"?hasTag(g,"gala") …}
340:   if(state.tab==="toa"&&!hasTag(g,"toa-dam"))return false;
341:   if(state.tab==="gal"&&!hasTag(g,"gala"))return false;
```

Đo trên trình duyệt thật, cùng một tài khoản, cùng một lúc:

| shell | tab «Tất cả» | tab «Tọa đàm» | tab «Gala» | số **thẻ vẽ ra** ở tab Gala |
|---|---|---|---|---|
| `/crm` (v2) | 376 | **156** | **350** | **350** |
| `/crm/classic` | 376 | **149** | **303** | **303** |

L6 sửa đúng triệu chứng nhìn thấy (thiếu 2 link) nhưng **không** mang AC-A1 sang. Nếu sáng 08/08 phải lật `CRM_UI=classic` để cứu hộ thì **47 khách Gala đã đăng ký lại biến mất khỏi danh sách** — đúng lớp lỗi mà cả vé D028 sinh ra để diệt, còn nguyên trong làn dự phòng.

Giảm nhẹ: **hai trang cửa vẫn đúng** dù vào từ làn nào — bấm «Cửa Gala» **từ `/crm/classic`** vẫn mở ra **350 thẻ**, `#cTot`=`350`, banner «350 khách · 0 đã đến» (vì trang cửa gọi thẳng `?session=gala`). Nên hỏng là **danh sách tra cứu trên `/crm/classic`**, không phải công cụ điểm danh.

**Cách sửa gợi ý:** port đúng 5 dòng — `duBuoi()`/`tabCount()`/`buoiOf()` của `crm-app-v2.html:352,365,366,491` sang `crm-app.html`. Hoặc, nếu chấp nhận, ghi rõ vào runbook rằng làn classic đếm theo tag và **không** dùng để chốt số.

## N3 — **MEDIUM (nợ cũ L2, đổi chiều)** · `guests.js` và `stats.js` vẫn là hai luật, nay `guests.js` **chặt hơn**

**File:** `server/crm/guests.js:23-26` ↔ `server/crm/stats.js:92-97`

Worker đã tự ghi nhận trong thông điệp commit rằng chưa gộp được. QC xác nhận **vẫn còn**, và bản vá **đổi chiều lệch**: **5/26** chuỗi thử cho kết quả khác nhau.

| `sessions` | `guests.js` (quyết định **ai lên trang cửa**) | `stats.js` (quyết định **số KPI**) |
|---|---|---|
| `Galaxy` · `Bengala` · `Galas` | gala **false** | gala **TRUE** |
| `toa-dam` (slug) | toa **false** | toa **TRUE** |

Trước bản vá, `guests.js` **lỏng hơn** (`%ala%`) → rác lọt vào danh sách cửa. Sau bản vá, `guests.js` **chặt hơn** → một bản đăng ký ghi `Galaxy` sẽ **được KPI đếm vào Gala nhưng KHÔNG hiện ở tab Gala và KHÔNG lên `/checkin-gala.html`**. Đó **đúng là lớp lỗi vé D028 vừa sửa**, chỉ đảo chiều: im lặng, không dấu hiệu.

Hôm nay **0 dòng** bị ảnh hưởng (`sessions` chỉ có 3 giá trị). **Nhưng vẫn là do dữ liệu, không do cấu tạo** — y như kết luận vòng trước.

**Cách sửa gợi ý:** một file `server/crm/session-rule.js` export `SESSION_RE`, `guests.js` và `stats.js` cùng `require`; `stats.js` phân loại nhóm form bằng **chính biểu thức SQL** thay vì `classifySessions()` viết tay trong JS.

## N4 — **LOW** · regex mới bỏ sót vài biến thể gõ tay

**File:** `server/crm/guests.js:24`

`t[oọ]a[ ]?đ[aà]m|toa dam` chỉ chấp nhận **đúng một** dấu cách, và chỉ hai bộ dấu:

| người gõ | khớp? |
|---|---|
| `Toa  dam` (hai dấu cách) | **false** |
| `Toadam` (dính liền) | **false** |
| `Tòa đàm` (`òa` thay `ọa` — lỗi bỏ dấu rất phổ biến) | **false** |

`sessions` là free text từ form công khai (`rsvp.js:39` chỉ `clean()`, `:72` chỉ kiểm không rỗng — **không whitelist**), nên các biến thể này khả dĩ. Hôm nay 0 dòng.

**Cách sửa gợi ý:** `[ ]?` → `[[:space:]]*` và `[oọòóõỏ]` cho nguyên âm; hoặc — tốt hơn nhiều — **whitelist `sessions` ở `rsvp.js:39`** (`['Tọa đàm','Gala']`) để chuỗi tự do không bao giờ quyết định ai lên trang cửa. Sửa gốc rẻ hơn sửa regex.

## N5 — **LOW (chưa vá)** · NFD vẫn làm khách rơi khỏi Tọa đàm

**File:** `server/crm/guests.js:24`

```
normalize('Tọa đàm · Gala', NFD)  →  toa MỚI = FALSE · gala MỚI = TRUE
```

Bản vá đổi `ILIKE '%ọa đàm%'` thành regex nhưng **vẫn so trên chuỗi thô**. Client gửi dạng NFD (`o` + dấu tổ hợp) thì `t[oọ]a` không khớp vì giữa `o` và `a` còn một ký tự tổ hợp. Khách vào Gala nhưng **biến mất khỏi Tọa đàm** — im lặng.

Prod hôm nay **0 dòng non-NFC** (đo `sessions <> normalize(sessions, NFC)`). **Cách sửa gợi ý:** `normalize(s.sessions, NFC) ~* $n`, hoặc chuẩn hoá NFC ngay ở `rsvp.js` trước khi ghi.

## N6 — **LOW** · header sticky trên điện thoại cao thêm 48px; thứ tự focus khác thứ tự nhìn

**File:** `server/crm/views/crm-app-v2.html:187-191` (và bản sao ở `crm-app.html:177-183`)

- **Cao thêm:** `.top` ở 390px: **182px → 230px** (+48). `.top-in` **54 → 129**. Nguyên nhân: `flex-wrap:wrap` không chỉ đẩy `.cknav` xuống hàng 3 mà còn đẩy `.top-r` (日本語 · ☾ · ⋯) xuống hàng 2 — vì `.role` kết thúc ở x=269 mà `.top-r` rộng 140px, không còn chỗ. Ở 390×844 header chiếm **27%** màn. Đây là **cái giá đã trả để không mất nút**, chấp nhận được; muốn rẻ hơn thì phải thu `.brand`/`.role`/ngũ hành ở mobile. Desktop **không đổi** (120px).
- **Thứ tự focus:** `order:9` chỉ đổi thứ tự **nhìn**, không đổi thứ tự **DOM**. Bàn phím / trình đọc màn hình đi `brand → Cửa Tọa đàm → Cửa Gala → 日本語 → ☾ → ⋯` trong khi mắt thấy `日本語 ☾ ⋯` **trên** hai link. Lệch nhẹ, không chặn.
- Ở **280px** `/crm/classic` có `doc.scrollWidth 286` (tràn 6px) — thủ phạm là `span#ctGal.ct` và các `span.tag` trong dòng khách, **không phải `.cknav`**. 280px hẹp hơn mọi điện thoại đang bán; pre-existing, không do bản vá.

## N7 — **INFO** · `SESSION_RE` được nối chuỗi vào SQL ở nhánh `du_*`

**File:** `server/crm/guests.js:66, 70`

```js
AND s.sessions ~* '${SESSION_RE['toa-dam']}'   // nối chuỗi
AND s.sessions ~* $${formIdx}                  // ở nhánh ?session= thì lại dùng placeholder
```

`SESSION_RE` là **hằng do lập trình viên viết**, không chứa dấu nháy, nên **không có rủi ro injection hôm nay**. Ghi lại vì hai nhánh dùng hai kiểu khác nhau cho cùng một giá trị — ai sửa sau này dễ tưởng chỗ nối chuỗi cũng an toàn với giá trị từ ngoài.

---

# Đã thử phá mà không phá được

| Đòn | Kết quả |
|---|---|
| **Ép lại `flex-wrap:nowrap`** để dựng lại lỗi cũ (A/B trên chính trang live) | Lỗi quay lại **y nguyên** ở 390/360/320 (doc 565, ba nút văng khỏi màn). Tắt ép → hết. **Nhân-quả khép kín.** |
| **Gỡ hẳn `.cknav`** (giả lập trước D028) | Ở 360/320 **vẫn tràn** (370) và `#menu` (chứa Đăng xuất) vẫn văng — bản vá **tốt hơn** cả trạng thái trước vé, chứ không chỉ hoàn tác. |
| **Quét 20 bề rộng 280→1440 × 2 shell** tìm khoảng «kẹt giữa hai luật» | **0/40 vỡ.** Chuyển pha sạch tại đúng 640/641. |
| **Nhãn tiếng Nhật dài hơn** (bấm 日本語, đo lại ở 390 và 320, cả hai shell) | `受付 座談会` / `受付 ガラ`: `scrollWidth == clientWidth`, `doc.scrollWidth == innerWidth`, 3 nút in-view. Không tràn. |
| **Phóng chữ hệ thống lên 20px** (a11y) rồi **cộng thêm** tiếng Nhật, ở cả dọc và **ngang 844×390** | 12/12 tổ hợp: `topin sw==cw`, `offs=[]`, `cut=0`. Header 230→253px, không vỡ. |
| **Chuỗi rác khớp nhầm buổi** — 26 chuỗi trên Postgres, gồm `Salad` `Balalaika` `Galaxy` `Bengala` `Galas` `MASALA` | **0 khớp nhầm.** Biên từ giữ được cả tiếp đầu lẫn tiếp đuôi. |
| **Regex mới đổi kết quả trên dữ liệu thật?** | `gala_doi_phe 0` · `toa_doi_phe 0` — không một khách nào xê dịch. |
| **Ép `?session=` và `du_*` lệch nhau bằng tổ hợp tham số** — `limit` nhỏ, `q=`, `mine=1`, `q=%` (ký tự wildcard) | Không lệch, không 500. `session=gala&limit=10`→10/10 đúng cờ · `session=toa-dam&limit=3`→3/3 · `session=gala&q=nguy&limit=1000`→98/98 · `session=toa-dam&q=nguy&limit=1000`→48/48 · `session=gala&q=%`→50/50 · `session=gala&mine=1`→0 (agent smoke không có phân công). |
| **So tập hợp ID** thay vì tổng, 4 phép | 4/4 `chỉ-API=0, chỉ-SQL=0`. Bằng nhau tuyệt đối, ở **hai** lần chụp dữ liệu khác nhau. |
| **Rò token buổi** `session=gala-vip`, `session=GALA` | 400 cả hai — whitelist `SESSION_TAGS` chặn trước SQL. |
| **Regex `~*` có làm chậm / N+1 không** — `EXPLAIN (ANALYZE, BUFFERS)` ×5 | **Không.** `SubPlan → Seq Scan on rsvp_submissions (rows=8, loops=1)` — hashed SubPlan, dựng một lần, **không** rescan 378 dòng. Execution Time 5 lần: `1.259 · 1.377 · 1.390 · 1.472 · 1.576 ms`, trung vị **1.390 ms**. `Buffers: shared hit=31`, **0 read**. |
| **Chấm nhầm bản** (working tree đang bẩn vì worker viết tiếp AC-B2/B3) | Phát hiện kịp: `diff` với **blob của `c104ae9`** chứ không với file trên đĩa → 4/4 IDENTICAL. Nếu so với working tree sẽ báo DIFFERS **sai**. |
| **Số trôi trong lúc chấm** (376 → 378 khách) | Chụp lại toàn bộ; bất biến 0-lệch đúng ở **cả hai** mốc. SQL xác nhận **2** khách mới `created_at` trong 90 phút — trôi thật, không phải lỗi luật. |
| **Lỗi JS** trên `/crm`, `/crm/classic`, `/checkin-gala.html` | **0** ở mọi lần mở, mọi viewport. |
| **`.n1`/`.n2` báo `scrollWidth > clientWidth`** trong lúc quét | Dương tính giả — CSS `:132` cố ý `white-space:nowrap;overflow:hidden;text-overflow:ellipsis`. Không phải tràn. |

---

## §B7 — ai code / ai QC

Worker viết `e7566d5` · `f9c7b1d` · `c104ae9`: **PM-con-Kha (R1)** — cùng người đang commit tiếp AC-B2/B3 trong working tree lúc lượt chấm này chạy; QC chấm lại AC-B0 cho tip `c104ae9`: **actor QC độc lập, không phải worker**, chỉ Read + Grep + Bash read-only, mọi SQL bọc `BEGIN TRANSACTION READ ONLY` + `ROLLBACK`, không sửa file nào ngoài chính báo cáo này, không commit, không deploy, không ghi DB, không in token và không in tên/SĐT khách — đúng §B7.

---

## Tóm cho PMt

- **Bản vá CSS đạt, và đạt sạch.** 40/40 phép đo ở 20 bề rộng × 2 shell không tràn một px nào; 日本語 · sáng/tối · `⋯` (có **Đăng xuất**) nay nằm trong màn ở **390 · 360 · 320**; hai link hiện đủ chữ; desktop y như cũ. Còn tốt hơn trạng thái trước D028, vì ở 360/320 thì `⋯` **vốn đã** văng khỏi màn từ trước. **Không cần đụng lại phần này.**
- **`%ala%` → regex biên từ đạt.** 26 chuỗi thử, 0 rác lọt, và trên dữ liệu thật không một khách nào xê dịch.
- **Chặn PASS là N1 — và nó là hồi quy do chính bản vá.** `buoiOf` đổi sang `du_*`, nhưng hồ sơ khách đọc từ `GET /crm/guests/:id` — endpoint **không trả** hai cột đó. Kết quả: hồ sơ nay ghi «BUỔI: —» cho **376/376** khách, trong khi trước bản vá **305** người hiện đúng. Sửa ở máy chủ (`guests.js:99`, copy 8 dòng `SELECT` đã có sẵn) là gọn nhất. **File Xuất CSV thì đã đúng** — 350/156 khớp.
- **Nên vá cùng lượt: N2.** `/crm/classic` có 2 link nhưng danh sách vẫn đếm theo tag (**149/303** thay vì **156/350**). Lật `CRM_UI=classic` giữa buổi là mất 47 khách Gala khỏi danh sách tra cứu. Hai trang cửa thì **vẫn đúng** dù vào từ làn nào — nên đây là rủi ro tra cứu, không phải rủi ro điểm danh.
- **N3 (hai luật buổi ở hai file) vẫn là nợ, và bản vá đã đảo chiều nó:** nay `guests.js` chặt hơn `stats.js`, nghĩa là nếu có chuỗi lạ thì KPI sẽ đếm mà trang cửa thì không — đúng lớp lỗi D028 vừa diệt. Hôm nay 0 dòng dính. Ghi vé riêng, kèm N4/N5 (whitelist `sessions` ở `rsvp.js` giải quyết cả ba cùng lúc).
- **Regress sạch:** 0 lệch tới từng ID ở hai mốc dữ liệu khác nhau, 400/401/403 đúng, `smoke:crm` **43/43**, `EXPLAIN ANALYZE` cho thấy regex không gây N+1 (trung vị 1.39 ms).
- **Vẫn chưa có bằng chứng live cho `btl`.** `staff_users` vẫn `btl: 4` và **không có** dòng `staff`; role tôi đo được (`staff`, qua làn Bearer smoke) là role không người thật nào dùng. Với N1 điều đó **không** làm nhẹ đi: ô «Buổi» không phụ thuộc role, cả hai role đều thấy «—».
