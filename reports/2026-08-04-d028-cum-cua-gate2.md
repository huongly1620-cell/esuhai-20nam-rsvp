# Gate 2 QC — E08-D028 cụm cửa (A · B0 · B2 · B3 · C · N1 · N2)

> # VERDICT: **FAIL**
>
> **Hai lỗi mức `blocker` còn SỐNG trên production tại thời điểm chấm** — cả hai đều nằm đúng trên màn PG cầm ở cửa ngày 08/08, và tôi tái hiện được cả hai bằng trình duyệt thật trên **chính byte prod đang phục vụ**, ở **hai bản deploy khác nhau** trong cùng phiên QC.
>
> - **CỬA-1 · Không đóng được hồ sơ khách.** Banner cụm cửa (`z-index:99999`) đè kín nút «←»; quét 648 điểm khắp hộp nút → **0 điểm chạm được**; chạm ngón tay thật → hồ sơ vẫn mở. Mỗi khách xong phải **tải lại trang**.
> - **CỬA-2 · Cửa báo xanh dù máy chủ TỪ CHỐI.** POST check-in trả 500 / 401 / 404 → màn vẫn hiện «◯ Đã check-in lúc ✓», bộ đếm +1, banner +1, thẻ chuyển «đã đến», **không một thông báo lỗi nào**, DB **không có dòng nào**.
>
> **Hồi quy kiểu N1 = FAIL** — theo luật này thì vòng chấm trước (khi hai trang cửa còn đọc «Buổi» theo tag, để trống ô BUỔI cho 49/352 và 7/156) đúng ra phải FAIL chứ không «PASS có điều kiện». Lỗi đó **nay đã được vá** (`e425d22`) và tôi xác nhận trên bản live: **0/352 và 0/156 ô rỗng** — nhưng verdict lần này FAIL vì hai blocker ở trên, không vì N1.

**§B7 — ai code / ai QC:** người viết mã là worker đã commit `e7566d5 · 1fa2ead · f9c7b1d · c104ae9 · 8083d33 · e249623 · e425d22 · c09859f`; người chấm là **phiên QC độc lập thứ tư**, không commit bất kỳ dòng nào của vé này, không sửa file, không deploy, không ghi DB — mọi SQL bọc `BEGIN TRANSACTION READ ONLY … ROLLBACK`, mọi phép thử ghi chạy trên máy chủ giả với khách tổng hợp («TEST GUEST nn»).

**Mốc đo (UTC / giờ VN +7):** 12:03Z–12:15Z ≈ **19:03–19:15 ngày 04/08/2026**. Prod: `https://esuhai-web-production.up.railway.app`.
**Vệ sinh dữ liệu:** báo cáo chỉ chứa số đếm, id nội bộ và tên cột — không tên/SĐT/email khách, không token, không chuỗi kết nối. Toàn bộ file tạm của phiên này đã xoá (`scratchpad/qcFinal/` + 3 file HTML tải về), đã kiểm lại: sạch.

---

## 0. CẢNH BÁO TRƯỚC MỌI THỨ — bản đang chấm KHÔNG còn là bản đang chạy

Vé giao chấm tip `e249623`. Thực tế trong phiên:

| Mốc | `/checkin-gala.html` prod (sha256, 16 ký tự đầu) | Là bản nào |
|---|---|---|
| 12:03Z | `319f2c24afe0bbec` | **không khớp blob của bất kỳ commit nào** — khớp đúng **cây làm việc chưa commit** |
| 12:12Z | `719bc4b730ed6da2` | = cây làm việc mới = `HEAD 7c3c57f` |

Đối chiếu blob: `e249623` → `d7fa296f…` · `e425d22` → `2cf47a6d…` · `c09859f` → `41f3d82d…`. **Không mốc nào bằng `e249623`.**

Trong lúc tôi đo, `git log` chạy từ `c09859f` → `3623cc3` (AC-E, tiếng Nhật) → `7c3c57f`, và prod được deploy lại ít nhất một lần. Hai hệ quả phải ghi vào biên bản:

1. **Có lúc production chạy mã chưa nằm trong git** (mốc 12:03Z). Nếu cần rollback lúc 20:00 ngày 08/08 thì không có commit nào để quay về.
2. **D và E đã được mở khi cụm cửa chưa qua Gate 2** — `c09859f` là AC-D (naturalWidth), `3623cc3` là AC-E (JP), đúng hai hạng mục PMt ghi HOLD «mở sau khi cụm cửa khoá». Cụm cửa hiện **chưa khoá**.

Mọi kết luận dưới đây được đo trên **byte prod thật tại mốc ghi kèm**, không đo trên file đĩa.

---

## 1. Bảng AC

| AC | Kết quả | Bằng chứng ngắn (tôi tự chạy) |
|---|---|---|
| **A** (A1 một SoT · A2 0 lệch) | ✅ **ĐẠT** | 4 nguồn gom trong **một** lượt `Promise.all` lúc **12:06:08Z**: tab (`du_*` client đếm) **352 / 156** · `?session=` **352 / 156** · KPI `/crm/stats` **303+42+7 = 352** và **149+7 = 156** · SQL tôi tự viết (luật khác code: tag bằng `ILIKE ',x,'`, biên từ `[^[:alnum:]]` thay `[^[:alpha:]]`) lúc **12:07:55Z** → **352 / 156**. `invited 378` ở cả 4 nguồn. `integrity.partitionOk/disjoint/bucketsOk = true`, `overlapTagAndForm = 0`. **Lệch = 0.** |
| **B0** (2 link cửa, cả 2 làn) | ✅ **ĐẠT** | HTML prod của `/crm` (77 735 B) và `/crm/classic` (64 611 B) đều chứa đủ `checkin-gala.html` + `checkin-toadam.html`. Phần đo tràn header ở 7 viewport × 2 làn × vn/jp giữ nguyên kết quả vòng trước (0/28 tràn). |
| **B2** (phân vai điểm danh) | ✅ **ĐẠT** (có điều kiện câu chữ) | Cả hai làn prod nay đều có cổng `me.role==="btl" ? #ciBtn : .ci-off` (1 lần/file) **và** khoá `ci_at_door` (3 lần/file) → khoảng trống F3 của vòng trước **đã vá** (`e425d22`). ⚠️ Nhưng **ẩn nút ≠ chặn quyền**: `POST /crm/guests/:id/check-in` chỉ có `requireCrmAuth`, không `requireRole` — phải ghi đúng vào runbook (xem `i2`). ⚠️ Vẫn **chưa có** bằng chứng phiên `btl` thật (vòng thứ 3 liên tiếp): `staff_users` = **4 btl / 0 staff**. |
| **B3** (banner = dụng cụ cửa) | ❌ **KHÔNG ĐẠT** | Chữ thì đúng («✔ Cửa Gala · điểm danh ghi thẳng vào /crm · N khách · M đã đến», không còn «BẢN THIẾT KẾ»). Nhưng **chính banner làm hỏng dụng cụ cửa**: nó đè nút «←» (**CỬA-1, blocker**) và nuốt hàng đầu header sau khi cuộn, giết nút 日本語 và 📅 Lịch trình (**CỬA-3, major**). Số trên banner còn là ảnh chụp riêng của từng máy, không bao giờ tự làm mới (**CỬA-4, major**). |
| **C** (C1 chụp ảnh · C2 ảnh hỏng không đụng điểm danh) | ⚠️ **ĐẠT phần chính, còn 5 lỗi nhỏ** | C2 giữ vững: 12 ca ảnh hỏng (500 JSON · 401 · 503 · 500 thân HTML · TCP reset) × 2 thứ tự đều **không** xê dịch trạng thái điểm danh. C1 chạy đúng chuỗi client → 201 → thay ảnh tại chỗ. Còn: thông báo tiếng Anh «upload error» (`m3`), >12 MiB → 500 HTML (`m4`), HEIC chỉ chặn ở 2 trang cửa (`m5`), gửi lại POST khi đứt mạng (`i4`), ảnh của khách đã xoá vẫn tải được (`i5`). |
| **N1** (nhãn buổi đọc SoT, không đọc tag) | ✅ **ĐẠT** — vá trong lúc QC | Trên bản live: `buoiOf(r)` nay đọc `du_toa_dam`/`du_gala` ở **cả hai trang cửa** (`checkin-*.html:116`) và ở cả `/crm` + `/crm/classic`. Tôi tính lại nhãn bằng đúng hàm live trên payload thật 12:06:08Z: **ô rỗng = 0/352 (Gala) và 0/156 (Tọa đàm)** — trước đó là 49 và 7. |
| **N2** (làn classic bằng làn mới) | ✅ **ĐẠT** | `/crm/classic` = `/crm` = **378 / 156 / 352**; SQL độc lập của tôi cùng mốc cũng **378 / 352 / 156**. Không còn 149/303. |

**Ngoài bảng nhưng phải nêu:** **AC-B1** («hai trang cửa là chỗ PG điểm danh CHÍNH») **KHÔNG ĐẠT** — CỬA-2 làm màn cửa nói dối về việc đã ghi, CỬA-1 làm không quay lại được danh sách. Đây là lý do chính của verdict FAIL.

---

## 2. Lỗi còn lại, sắp theo mức nặng

> Số dòng dưới đây lấy trên **bản đang LIVE lúc 12:12Z** (`719bc4b7…` = `HEAD 7c3c57f`). File đang bị sửa liên tục nên tôi trích kèm nội dung để định vị được kể cả khi dòng trôi.

### 🔴 BLOCKER

#### CỬA-1 — Nút «←» đóng hồ sơ khách KHÔNG bấm được; mỗi khách xong phải tải lại trang
`checkin-gala.html:84` và `checkin-toadam.html:84` (banner `position:sticky;top:0;z-index:99999`) ↔ `checkin-gala.html:44` / `checkin-toadam.html:44` (`.pf{position:fixed;inset:0;z-index:60}`) ↔ `:100` (`<button class="pf-back" id="pfBack">`)

**Vì sao quan trọng.** Luồng thật ở cửa là: tra tên → mở hồ sơ → điểm danh (hoặc chụp ảnh) → **quay lại danh sách đón khách kế**. Bước cuối hiện không làm được bằng ngón tay. Lối ra duy nhất là tải lại trang — kéo theo mất ô tìm đang gõ, mất bộ lọc, và nạp lại 352 thẻ qua wifi hội trường, **cho mỗi khách**. Với hàng người đang xếp, đây là tắc cửa. Trung thực: **không phải hồi quy do D028** — banner sticky `z-index:99999` đã có từ D019, và ở bề rộng điện thoại nút đã bị che từ trước. Nhưng D028 là vé biến hai trang này thành dụng cụ cửa chính thức, và nó cho thấy **chưa ai từng mở hồ sơ rồi thử đóng lại trên hai trang này**.

**Bằng chứng tôi tự chạy** (Chromium/Playwright, viewport 390×844 `is_mobile + has_touch`, phục vụ **đúng byte prod**, API là máy chủ giả với 60 khách tổng hợp — không chạm prod):

```
banner: position=sticky  z-index=99999  cao 71.7px
.pf   : position=fixed   z-index=60
#pfBack: hộp y 12 → 50
elementsFromPoint(tâm nút) = [DIV|z=99999, BUTTON.pf-back, DIV.pf-bar]
quét 648 điểm khắp hộp nút     → reachable_pts = 0
touchscreen.tap đúng tâm       → .pf VẪN mở
#pfBack.click() bằng JS        → .pf ĐÓNG NGAY  ⇒ handler lành, thuần bị che
```

Lặp lại **4/4**: `checkin-gala.html` + `checkin-toadam.html` × hai bản byte prod (`319f2c24` lúc 12:03Z và `719bc4b7` lúc 12:12Z). 0 lỗi JS.

**Cách sửa đề nghị (ĐỪNG tự sửa — chờ PMt duyệt).** Một dòng CSS: hạ banner xuống dưới mọi lớp nổi — `z-index:99999` → **`z-index:1`** (banner là phần tử đầu tài liệu nên vẫn nằm trên nội dung thường). Cách này **chữa luôn CỬA-3**. Sau khi vá phải đo lại: `elementFromPoint` tại tâm `#pfBack` phải trả về chính `#pfBack`, và **chạm thật** phải đóng được hồ sơ ở 360 / 390 / 430 trên **cả hai** trang.

#### CỬA-2 — Cửa báo «Đã check-in» dù máy chủ đã từ chối; Postgres không có dòng nào
`checkin-gala.html:224` và `checkin-toadam.html:224` — cùng một dòng, cùng một lỗi:

```js
jpost("/crm/guests/"+id+"/check-in").then(function(r){
  var g=byId(id); if(g) g._came=(r.body&&r.body.at)||g._came||"1";
  counter(); openPf(id); renderList();
}).catch(function(){ b.disabled=false; });
```

`jpost` (`:110`) trả `{ok,status,body}` và **không ném** khi `!r.ok` (khác `jget` ở `:109` vốn `throw`). Dòng trên **không đọc `r.ok`/`r.status` một lần nào**; thân lỗi không có `at` nên `g._came` rơi vào hằng `"1"`, và `fmtCame` in ra dấu «✓».

**Vì sao quan trọng.** Đây là cửa hỏng thật mà mọi đèn đều xanh. Khách đứng trước mặt, PG bấm, màn báo đã đến, banner cộng lên — `crm_check_ins` trống. Không ai ở cửa có cách nào nhận ra, vì bộ đếm trên màn là đếm **phía client**, cộng theo cú bấm chứ không theo cái đã ghi. Phiên CRM đóng cứng `exp` lúc verify OTP và **không có chỗ nào cấp lại token** — đăng nhập 06:00 là chết lúc 18:00, ngay giữa ca Gala 17:00–21:00; từ đó trở đi **mọi** khách đều mất mà banner vẫn leo. Nặng hơn: phép kiểm tay duy nhất trong runbook (`reports/2026-08-04-checklist-ra-le.md:52` — «Điểm danh thử 1 khách rồi kiểm số đã đến nhảy đúng») **bị chính lỗi này vô hiệu**, vì số «đã đến» nhảy đúng kể cả khi ghi hỏng.

**Bằng chứng tôi tự chạy** (cùng khung như trên; chỉ đổi mã trả về của `POST /crm/guests/:id/check-in`):

| HTTP máy chủ trả | Hồ sơ hiện | `#cDone` | Banner | Thẻ | Toast | **Máy chủ lưu** |
|---|---|---|---|---|---|---|
| 500 | «◯ Đã check-in lúc ✓» | 1 | «60 khách · 1 đã đến» | `.gc.done` | **không** | **0 dòng** |
| 401 | «◯ Đã check-in lúc ✓» | 1 | «60 khách · 1 đã đến» | `.gc.done` | **không** | **0 dòng** |
| 404 | «◯ Đã check-in lúc ✓» | 1 | «60 khách · 1 đã đến» | `.gc.done` | **không** | **0 dòng** |

Nút điểm danh biến mất ở cả 3 ca (`ci_btn_left = 0`). Lặp trên **cả hai trang cửa** và **cả hai bản byte prod**. 0 lỗi JS. Ba mã lỗi đều tới được thật: 401 khi hết phiên, 500 khi lỗi DB (`server/crm/guests.js` nhánh catch), 404 khi khách bị xoá mềm trong lúc cửa giữ danh sách cũ.

**Cách sửa đề nghị.** Kiểm `r.ok` trước khi đụng `_came`; hạ tầng báo lỗi **đã có sẵn trong file** (`toast(msg,1)` + CSS `.ci-toast.bad`, `sendPhoto` đang dùng):

```js
if(!r.ok){ b.disabled=false;
  toast(r.status===401 ? t("cam_auth") : "Chưa ghi được điểm danh — thử lại", 1); return; }
```

Và nên đối chiếu lại từ máy chủ sau mỗi lần ghi (như `/crm` đang làm bằng `loadAll()`), để màn cửa không bao giờ tự khẳng định trạng thái mà máy chủ chưa xác nhận.

### 🟠 MAJOR

#### CỬA-3 — Cuộn danh sách là banner nuốt luôn hàng đầu header: nút 日本語 chết, chạm 📅 Lịch trình có thể nhảy sang `/crm`
`checkin-*.html:84` (banner `z-index:99999`) ↔ `:15` (`.top{position:sticky;top:0;z-index:30}`) ↔ `#langBtn`, `#lichBtn`

Nút 日本語 là **đường duy nhất** để PG phục vụ khách Nhật đổi giao diện — đúng thứ hai trang cửa được xây để phục vụ. Với 352 khách thì PG cuộn gần như liên tục, nên trạng thái «đã cuộn» là mặc định chứ không phải ngoại lệ.

**Tôi tự chạy** (cùng khung, `window.scrollTo(0,900)`), **3/3 viewport**:

| viewport | banner cao | `.top` z | `#langBtn` hộp | `elementFromPoint(tâm)` | chạm thật → đổi ngôn ngữ? |
|---|---|---|---|---|---|
| 360×740 | 71.7 | 30 | y 10 → 40.7 | `DIV` (banner) | **không** |
| 390×844 | 71.7 | 30 | y 10 → 40.7 | `DIV` (banner) | **không** |
| 430×932 | 71.7 | 30 | y 10 → 40.7 | `DIV` (banner) | **không** |

`#lichBtn` cùng hộp y 10 → 40.7, cũng bị banner nhận chạm. Lăng kính «cửa anh» còn đo được ca nặng hơn (chạm hụt rơi vào link `Mở /crm` trong banner → **rời hẳn trang cửa** giữa lúc đón khách); ở toạ độ tâm nút tôi chỉ đo được «chết», không đo được «nhảy trang» — ghi cả hai cho công bằng.

**Cách sửa.** Cùng bản vá với CỬA-1 (hạ `z-index` banner xuống dưới 30). Nếu buộc giữ banner nổi thì phải đẩy `.top{top:…}` và `.sr{top:…}` theo chiều cao banner — mà chiều cao đó đổi theo bề rộng (40 px khi 1 dòng, 66–72 px khi 2 dòng), nên hạ `z-index` rẻ và chắc hơn nhiều.

#### CỬA-4 — «N khách · M đã đến» là số RIÊNG của từng máy và không bao giờ tự làm mới
`checkin-*.html:129` (`counter()` ghi `#cDone`/`#cTot`/`#bnTot`), chỉ được gọi ở `:234` (`boot()`, chạy đúng một lần) và `:224` (sau khi **chính máy này** điểm danh)

**Tôi tự chạy trên byte live:** `grep -c "setInterval|visibilitychange|EventSource|WebSocket|pageshow"` → **0** ở cả hai trang cửa. Không có bất kỳ cơ chế làm mới nào.

Cửa Gala 352 khách chắc chắn dùng nhiều hơn một điện thoại → mỗi máy báo một con số, **không máy nào báo đúng**, và không có dấu hiệu nào cho biết số đã cũ. Rủi ro sắc nhất là **tổng bị đóng băng**: máy mở lúc 17:00 ngày 08/08 sẽ không chứa khách đăng ký sau đó — PG tra ra «Không tìm thấy» và không điểm danh được nếu không biết phải tải lại. (Prod đã trôi 376 → 378 chỉ trong hôm nay.) Giảm nhẹ: `POST check-in` idempotent (`UNIQUE (guest_id)` + `ON CONFLICT DO NOTHING`, lần hai trả `already:true`) nên **dữ liệu không hỏng**, chỉ thứ PG nhìn thấy là sai.

**Cách sửa.** `setInterval(reload, 20000)` + `visibilitychange` gọi lại đúng đoạn `GET /crm/guests?session=…&limit=1000` của `boot()` rồi `counter()+renderList()` (giữ `st` để không mất bộ lọc). Nếu không kịp: ghi giờ chụp lên banner («số lúc HH:MM») và ghi vào runbook rằng số trên banner là số của riêng máy đó.

#### KIỂM-1 — «0 lệch» và «smoke 43/43» đều MÙ với cụm cửa
`tools/smoke-crm.js` (toàn file) · `tools/smoke-crm-ui.py` (chỉ mở `/crm`)

**Tôi tự chạy:** `grep -c "checkin\|session=" tools/smoke-crm.js tools/smoke-crm-ui.py` → **0 và 0**. Và `npm run smoke:crm` ở chế độ đầy đủ (có `DATABASE_URL` → bật đối chiếu SQL; có bearer → bật phần UI) → **`PASS — 43/43`**, trong đó `AC-5 số thẻ UI khớp invited — UI 378 vs API 378` đọc tab mặc định `all`, tức không đụng dân số buổi.

Nghĩa là: **lớp lỗi mà D028 sinh ra để diệt** (khách đã đăng ký biến khỏi trang cửa mà màn không có dấu hiệu) **vẫn có thể tái diễn nguyên vẹn với 49 người hôm nay, và toàn bộ bộ máy kiểm chứng vẫn xanh**. Bảng «0 lệch» hiện chỉ tồn tại vì QC gõ tay từng vòng — sáng 08/08 sẽ không ai chạy nó.

**Cách sửa (3 dòng, không đụng mã sản phẩm).** (1) gọi `?session=gala` và `?session=toa-dam`, ghim SÀN (vd 340 / 150) hoặc so với SQL union độc lập; (2) `record('AC-4 nhóm form không ai «chưa nêu buổi»', fk.unspecified === 0)`; (3) mở `/checkin-gala.html` + `/checkin-toadam.html` bằng Playwright và assert `#cTot` bằng đúng số `?session=` trả về. Song song thêm vào runbook mục 5 một dòng: «mở cửa Gala, đọc banner, phải đúng 352».

### 🟡 MINOR

| # | Vị trí | Vì sao quan trọng | Cách sửa đề nghị |
|---|---|---|---|
| `m1` | `server/crm/guests.js` (toàn file — không có `DELETE/UPDATE` trên `crm_check_ins`) · `checkin-*.html:59,98` (chuỗi `undo` chết) · `crm-app-v2.html` khối `.ci-on` | **Điểm danh nhầm thì KHÔNG role nào lùi lại được.** `grep -rn 'DELETE FROM' server/` → 1 hit duy nhất và là `crm_auth_codes`. Xoá khách là **soft delete** nên dòng check-in vẫn sống (prod đã có đúng 1 ca như vậy). Chuỗi dịch «Huỷ check-in» còn nằm trong bảng `T` khiến người viết runbook tưởng chức năng có sẵn. | Trước lễ chọn rẻ: ghi vào runbook cửa mục «điểm danh nhầm → gọi trực kỹ thuật» kèm sẵn `DELETE FROM crm_check_ins WHERE guest_id=$1`. Nếu muốn nút thì `DELETE /crm/guests/:id/check-in` + `requireRole('btl')` + audit — nhưng đó là scope mới, 4 ngày trước lễ thì runbook an toàn hơn. |
| `m2` | `checkin-*.html:224` — `.catch(function(){b.disabled=false;})` | **Rớt mạng đúng lúc bấm điểm danh: không một thông báo nào.** Trạng thái thì trung thực (khác CỬA-2), nhưng PG bấm mà «không có gì xảy ra» → phản xạ là bấm tiếp vài lần rồi bỏ qua khách. Đã đối chứng: cùng điều kiện đứt mạng, làn **ảnh** thì toast hiện bình thường — im lặng là riêng của nhánh này. | Trong `.catch` gọi `toast("Mất kết nối — chưa ghi được điểm danh, thử lại",1)`. Cùng chỗ sửa với CỬA-2, vá một lượt. |
| `m3` | `checkin-*.html:195` `toast(e.error||t("cam_fail"),1)` ↔ `server/crm/photos.js:38` `error:'upload error'` | **Lỗi máy chủ hiện nguyên văn tiếng Anh cho PG.** Đúng 8 phút MinIO chết như đã xảy ra hôm nay, mọi lần chụp ảnh sẽ hiện chữ đỏ «upload error» — không nói chuyện gì, và quan trọng nhất là **không nói «điểm danh vẫn giữ»** (câu trấn an mà nhánh lỗi mạng có, nhánh này không). | 5xx thì luôn dùng `t("cam_fail")` nối thêm «Điểm danh vẫn giữ.»; hoặc đổi `photos.js:38` sang câu tiếng Việt như các nhánh 400/503 anh em. |
| `m4` | `server/crm/photos.js:11-13` (`limits:{fileSize:12*1024*1024}`) · `server/index.js:15-52` (không có error handler 4 tham số) | **Ảnh > 12 MiB → Express trả 500 thân HTML → PG chỉ thấy «Không lưu được ảnh»**, không chữ nào nhắc dung lượng, nên phản xạ là chụp lại bằng chính máy đó và hỏng y hệt. Kệ ảnh của sự kiện đã có 3 dòng vượt hạn (lớn nhất 20,8 MB, vào qua bộ nạp hàng loạt). Xác suất thấp (máy ở cửa hiếm khi vượt), nhưng cơ chế đã xác nhận trực tiếp trên prod. | (1) kiểm `file.size` ở client trước khi gửi, câu báo có hành động; (2) thêm error handler JSON bắt `err.code==='LIMIT_FILE_SIZE'` → 413 + câu tiếng Việt. Nâng trần lên 25 MB cũng hợp lý. |
| `m5` | `checkin-*.html` (có chặn HEIC) ↔ `crm-app-v2.html` / `crm-app.html` (không chặn) ↔ `server/crm/photos.js:15` `ALLOWED` còn `'image/heic','image/heif'` | **Chặn HEIC chỉ có ở 2 trang cửa.** Ô input ở `/crm` có thuộc tính **giống hệt** (`accept="image/*" capture="environment"`), và `staff_users` hiện **4/4 là btl** — tức người thật chủ yếu đi đường `/crm`, đúng đường không được chặn. Loại ảnh «lưu được mà không hiện được» vẫn chui vào kệ. Kệ hôm nay còn sạch (202 ảnh, chỉ jpeg/png). | Đặt chốt ở máy chủ cho gọn: bỏ `'image/heic','image/heif'` khỏi `ALLOWED` (một dòng) + câu tiếng Việt có hành động. Cả 3 đường cùng một luật, khỏi copy regex sang chỗ thứ ba. |
| `m6` | `crm-app-v2.html:361` `loadAll()` · `:619` gọi một lần | **`/crm` không bao giờ tự cập nhật sau khi cửa điểm danh** — phải F5, và không có dấu hiệu nào cho biết số đang cũ (trong khi đồng hồ đếm ngược vẫn nhích, làm trang trông như đang sống). AC-B2 vừa biến `/crm` thành màn XEM KẾT QUẢ nên khoảng trống này thành nổi bật. | `document.addEventListener("visibilitychange",…)` gọi `loadAll()+loadStats()`; hoặc `setInterval(loadAll, 60000)`. Không kịp thì ghi runbook: «số trên /crm chỉ đúng tại thời điểm mở». |

### ⚪ INFO (ghi nhận, không chặn)

| # | Vị trí | Nội dung |
|---|---|---|
| `i1` | `server/crm/guests.js:33` (`Math.min(1000,…)`) · 4 nơi gọi `limit=1000` | **Cả 4 màn nạp danh sách bằng MỘT lần gọi không phân trang, server cứng cap 1000.** Hôm nay 378/1000 nên chưa chạm. Nhưng danh sách lớn theo **cụm import** (một lần sáng nay đẩy ~117 → 378; prefix `vnjb` 259 dòng trong một phát), và `import.js` không có trần số dòng. Vượt mốc thì tab / file Xuất / hai trang cửa **âm thầm cụt** trong khi KPI (`count(*)`, không LIMIT) vẫn đúng — đúng lớp «tab một đằng, KPI một nẻo». Rẻ nhất: khi `rows.length === limit` thì hiện cảnh báo trên `#statline`; đúng hơn: trả thêm `total`. |
| `i2` | `server/crm/guests.js:209` | **Trả lời dứt điểm câu «ẩn nút thì API còn chặn không»: KHÔNG chặn — và đó là ĐÚNG thiết kế.** Hai trang cửa gọi chính endpoint này bằng chính phiên đó, nên chặn staff ở API là giết luôn AC-B1. Việc cần tránh là **ghi vào Gate 2 / runbook** rằng «/crm đã chặn staff điểm danh». Câu đúng: «/crm ẩn nút với staff là rào **quy trình**, không phải rào **quyền**». Bằng chứng: cùng token staff, `POST …/check-in` trên id không tồn tại → **404**, `DELETE /crm/guests/…` → **403**. |
| `i3` | `checkin-gala.html:7` (`--acc:#2E7FCC`) ↔ `:84` (banner `#0f955a`) | Banner hai trang cửa **cùng một màu xanh lá**, kể cả trang Gala vốn xanh dương — dấu hiệu nhận dạng buổi mạnh nhất bị vô hiệu ở dải trên cùng. Không vi phạm AC nào (B3 chỉ chốt copy), phần thân trang vẫn đúng màu buổi. Xếp «gợi ý mỹ thuật» — **không nên đụng file đang LIVE vì việc này**. |
| `i4` | `server/crm/photos.js:19-40` (không có idempotency key) | **Đứt kết nối giữa chừng → trình duyệt tự gửi lại POST ảnh lần hai** (tái hiện được trên cả HTTP/1.1 lẫn **HTTP/2**, đúng giao thức prod; thân request trùng khít byte, và người dùng còn thấy toast **thành công**). Hậu quả nhẹ: kệ cho phép nhiều ảnh/khách, avatar lấy tấm mới nhất → không sai dữ liệu, chỉ phình kho. Prod hiện **chưa có** cặp trùng nào. Nếu vá thì phải vá **phía máy chủ** (`X-Idempotency-Key` hoặc dedup theo hash) — cờ đang-gửi ở client không chặn được vì việc gửi lại nằm dưới `fetch`. |
| `i5` | `server/crm/photos.js:43-55` | **`GET /crm/photos/:id` không kiểm khách đã xoá** — `10` ảnh của khách đã soft-delete vẫn tải được (đã đối chiếu size khớp `crm_photos`), trong khi `GET /crm/guests/:id` của đúng những khách đó trả 404. Phạm vi hẹp (chỉ trong CRM đã xác thực), có trước D028. Vá: thêm `JOIN crm_guests g ON g.id=p.guest_id AND g.deleted_at IS NULL`. |
| `i6` | scratchpad phiên QC (ngoài mã sản phẩm) | **Rác của các vòng QC trước chứa secret và PII.** Các vòng trước để lại file dump `railway variables` (chứa `CRM_SMOKE_BEARER`, `DATABASE_PUBLIC_URL`, `POSTGRES_PASSWORD`, `MINIO_SECRET_KEY`, `SMTP_PASS`, `ADMIN_PASSWORD`…) và các JSON chứa **376 họ tên + 42 SĐT** khách thật. Các lăng kính chạy song song **cố ý không xoá của nhau** để khỏi phá phiên đang chạy. **Đề nghị PMt: sau khi vòng này khép, chạy một lệnh dọn duy nhất `rm -rf <scratchpad>/*`, và xoay (rotate) `CRM_SMOKE_BEARER` trước 08/08** vì token đã ghi ra đĩa ở ít nhất 3 file. Phiên này: 0 file để lại (đã xoá `scratchpad/qcFinal/` + 3 HTML tải về, kiểm lại sạch). |
| `i7` | quy trình (mục 0) | **Prod có lúc chạy mã chưa commit** (12:03Z), và **D + E đã mở khi cụm cửa chưa khoá** (`c09859f` AC-D, `3623cc3` AC-E). Đề nghị: mọi deploy trước lễ phải đi từ một commit, và giữ đúng thứ tự HOLD của PMt. |

### ✅ Hai lỗi của vòng trước ĐÃ ĐÓNG (tôi xác nhận trên bản live)

- **Đ1 / N1 trên hai trang cửa** — `e425d22` đổi `buoiOf(a)` (đọc mảng tag) thành `buoiOf(r)` (đọc `du_toa_dam`/`du_gala` do máy chủ tính) và `mapRow` truyền `r`. Tôi tính lại nhãn bằng **đúng hàm đang live** trên payload thật lúc 12:06:08Z: **ô «Buổi» rỗng = 0/352 và 0/156** (trước: 49 và 7).
- **F3 / B2 trên làn rollback `/crm/classic`** — `e425d22` port nguyên khối cổng `me.role==="btl"` **kèm** khoá i18n `ci_at_door` (vn + jp) và CSS `.ci-off`. Tôi đo trên HTML prod của cả hai làn: cổng vai **1/1 file**, khoá `ci_at_door` **3/3 lần/file**, đủ 2 link cửa.

---

## 3. Đã thử phá mà không phá được

**Về hai blocker — tôi cố bác bỏ và thất bại:**

- «Prod khác bản đang chấm» → tôi không đo trên file đĩa mà **tải thẳng byte prod** và băm; rồi chạy repro trên **hai bản byte khác nhau** (`319f2c24` 12:03Z và `719bc4b7` 12:12Z, tức trước và sau lần deploy giữa phiên). Cả hai đều hỏng y hệt.
- «`elementFromPoint` chỉ nói thứ tự vẽ, chạm vẫn ăn» → chạm thật bằng `touchscreen.tap`: hồ sơ vẫn mở. Và `#pfBack.click()` bằng JS thì **đóng ngay** ⇒ handler lành, đây thuần tuý là che hit-test, không phải JS hỏng.
- «Chắc còn hở vài px dưới banner» → banner đáy 71.7 > nút đáy 50; quét **648 điểm** khắp hộp nút → **0** điểm chạm được.
- «Còn lối thoát khác (Escape, vuốt, Back)» → `grep` hai file: 0 lần `keydown`/`Escape`/`pushState`/`popstate`; `.pf` là `inset:0` nên không có «bên ngoài» để bấm.
- «500/401/404 không tới được thật» → 401 tới được vì phiên đóng cứng `exp` lúc verify OTP và **không có chỗ nào cấp lại**; 500 từ nhánh catch DB; 404 khi khách bị xoá mềm. Ngoài ra prod thật trả **401** cho `POST …/check-in` không auth — tôi đã đo bằng id không tồn tại nên không khách nào bị đụng.
- «Trang tự đồng bộ lại nên trạng thái giả sẽ tự sửa» → `grep` byte live: `setInterval|visibilitychange|EventSource|WebSocket|pageshow` = **0**. Trạng thái xanh giả sống hết ca.
- «`enrich()` sẽ ghi đè lại» → `enrich()` chỉ set `photo/diet/role/handler/seat`, không đụng `_came` (và không đụng `buoi`).
- «Chỉ đúng ở làn Bearer smoke (role staff), người thật role btl thì khác» → `GET /crm/guests` chỉ có `requireCrmAuth`, không `requireRole`, không `?mine` → cookie btl nhận rows y hệt.
- «Dữ liệu trôi nên số của QC sai» → **không**: 3 phép đo độc lập trong 2 phút đều **378 / 352 / 156** (API 12:06:08Z · SQL 12:07:55Z · smoke ~12:09Z), và `created_at > now()-90min` = **0** khách mới. Hai blocker lại là lỗi **hình học và luồng điều khiển**, độc lập hoàn toàn với số khách.

**Về phần ĐẠT — tôi cố tìm lệch và không tìm được:**

- Ép `?session=` lệch với cờ `du_*` bằng tổ hợp `q`/`limit`/`mine`/ký tự wildcard: 7 tổ hợp, **sai = 0**. (Ghi thật: hai cột này dùng **chung** hằng `SESSION_RE`, nên chúng gần như là một phép kiểm, không phải hai bằng chứng độc lập.)
- Khách **vừa có tag buổi vừa có `response_id`** (lớp làm vỡ công thức KPI): **0 khách**; và bất biến này được giữ **chủ động** (`sync-from-rsvp.js` gỡ tag khi gắn `response_id`), lại **hỏng an toàn** nếu vỡ (`crm-app-v2.html` hạ dòng tổng và báo «hai nguồn không còn rời nhau»).
- `response_id` mồ côi: **0**. Tag buổi viết biến thể hoa/thường/khoảng trắng: **0** dòng lệch. Hai luật buổi (`guests.js` regex ↔ `stats.js`) đổi phe trên dữ liệu thật hôm nay: **0** (N3 vẫn HOLD, chỉ xác nhận hôm nay không dòng nào dính).
- Viết SQL **cố ý khác code** (biên từ `[^[:alnum:]]` thay `[^[:alpha:]]`) để không lặp lại lỗi của chính code: vẫn ra **352 / 156**.
- Ép QC làm bẩn prod: máy chủ giả + khách tổng hợp cho mọi phép thử ghi; SQL bọc `READ ONLY … ROLLBACK`; sau phiên `crm_check_ins` = 2, `crm_photos` = 202 — **không một byte nào của tôi ghi vào prod**. (Ghi rõ: dòng check-in thứ 2 đã có sẵn trước khi tôi bắt đầu, do phiên khác; tôi không bấm nút điểm danh trên prod lần nào.)
- Tìm một `record()` trong bộ smoke «in ok mà không kiểm gì»: **không có**. Lỗ hổng của smoke nằm ở **phạm vi phủ** (KIỂM-1), không ở chất lượng từng assert.

**Vẫn còn thiếu, vòng thứ ba liên tiếp:** bằng chứng trên **phiên `btl` thật**. `staff_users` = 4 btl / 0 staff, đăng nhập bằng OTP gửi email người thật; QC không phát mã vào hộp thư người khác và không ký cookie giả bằng `CRM_SESSION_SECRET`. Mọi số liệu «btl» trong ba vòng đều là **giả lập phía client**, phải đọc đúng như vậy.

---

## 4. Điều kiện mở khoá

Cụm cửa **CHƯA khoá**. Không mở D, không mở E (mà thực tế cả hai đã bị mở trước — xem mục 0 và `i7`).

Để chuyển sang PASS, cần đúng ba việc:

1. Vá **CỬA-1** (một dòng `z-index`) — vá xong thì **CỬA-3** đóng theo.
2. Vá **CỬA-2** (kiểm `r.ok`, dùng `toast` đã có sẵn) — nên vá **`m2`** cùng lượt vì cùng một dòng.
3. Deploy **từ một commit** (không deploy cây làm việc), rồi đo lại **đúng 6 phép** sau trên byte prod mới:
   - `elementFromPoint` tại tâm `#pfBack` = chính `#pfBack`, và **chạm thật** đóng được hồ sơ — ở 360 / 390 / 430 × **cả hai** trang cửa;
   - sau khi cuộn 900 px, chạm `#langBtn` **đổi được ngôn ngữ** — cùng 3 viewport × 2 trang;
   - `POST check-in` trả 500 / 401 / 404 → màn **không** hiện «đã check-in», có toast đỏ, bộ đếm **không** nhích;
   - `?session=gala` = tab `du_gala` = KPI = SQL độc lập (đo lại vì số sẽ còn trôi tới 08/08);
   - ô «Buổi» rỗng = **0** trên cả hai trang cửa;
   - `npm run smoke:crm` → PASS, **và** đã thêm 3 phép kiểm cụm cửa của KIỂM-1.

Trước khi vá xong, nếu buộc phải dùng hai trang cửa ngày 08/08 thì runbook **phải** ghi hai câu: «đóng hồ sơ khách = tải lại trang» và «màn báo đã check-in **không** đảm bảo đã ghi — cuối mỗi giờ mở `/crm` đối chiếu số đã đến».
