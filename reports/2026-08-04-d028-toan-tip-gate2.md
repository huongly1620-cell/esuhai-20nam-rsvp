# Gate 2 QC — E08-D028 **toàn tip** (sau wave gộp)

> # VERDICT: **FAIL** — 9/10 nhóm đạt sạch, **1 mục bắt buộc chưa đạt**
>
> - **E-1 (chặn PASS):** ô tìm trên **2 trang cửa** vẫn **không** đánh chỉ mục field JP. Gõ `千葉` ở cửa → **0 kết quả**; cũng khoá đó trên `/crm` → **2**. Commit `00ee6b0` khai «ô tìm cả 4 màn… nay tìm được cả 3 field» nhưng `checkin-*.html:146` vẫn là `norm(g.name+" "+g.title+" "+g.org)`. Phiếu wave ghi rõ «ô tìm **4 màn**». Vá = **1 dòng × 2 file**.
> - **CỬA-4 (nợ cũ, còn nguyên):** số ĐÃ ĐẾN / CHƯA ĐẾN **không bao giờ tự làm mới** — 30 giây đứng yên: **0** request `/crm/guests`, trang **không có** `setInterval`. Hai PG hai máy cùng một cửa sẽ thấy hai con số khác nhau suốt buổi.
>
> Mọi thứ còn lại — A · B0 · B2 · B4 · B5 · C1b/C2 · D0 · D2 · E3/E4 · regress — **đạt**, và hai blocker cửa + Đ-D2 + D0b của các vòng trước **đã vá thật, đo lại trên byte prod**.

> **Tip chấm:** `811fba3` — **CR-25 tự kiểm sha256 byte prod ↔ blob HEAD, 4/4 khớp** (mục 0).
> **Mốc đo:** 04/08/2026 **20:30–20:44 (+07)** · prod `https://esuhai-web-production.up.railway.app`
> **Mốc dữ liệu (prod sống):** 378 khách active · 352 Gala / 156 Tọa đàm · **1** check-in hiệu lực · JP đã backfill (E3).
> **§B7:** worker là người commit `00ee6b0` + `811fba3` (và cả cụm D028); **người chấm là phiên QC độc lập**, không viết dòng mã nào của vé này, không sửa file ngoài chính báo cáo này, không commit, không deploy.
> **An toàn dữ liệu:** **không ghi một byte nào vào prod** — SQL bọc `BEGIN TRANSACTION READ ONLY`; phép thử ảnh chặn ở trình duyệt và trả lời thay máy chủ; trạng thái «đã điểm danh» / role `btl` mô phỏng bằng viết lại phản hồi; phép dò >12 MiB gửi tới **id khách không tồn tại** nên dừng trước khi ghi. Báo cáo chỉ có số đếm + id nội bộ + tên **công khai trên danh thiếp** của 1 khách Nhật dùng làm ca thử; không SĐT, không email, không token.

| Nhóm | Kết quả | Bằng chứng gọn |
|---|---|---|
| **A** | ✅ **ĐẠT** | tab `du_*` **352/156** = `?session=` **352/156** = KPI **352/156** · invited 378 · `disjoint=true` → **0 lệch** |
| **B0** | ✅ **ĐẠT** | 2 nút nav đúng href/`_blank`/`noopener`, **`scrollWidth===clientWidth`** ở **390·360·320** trên **cả `/crm` lẫn `/crm/classic`**, 2 link luôn trong màn |
| **B2** | ✅ **ĐẠT** | `/crm` + classic: nút điểm danh = **0** với **staff** *và* **btl**; khách đã đến vẫn hiện đủ kết quả «◯ Đã điểm danh · … · 18:50:53» |
| **B4** | ✅ **ĐẠT** | 2 cửa: banner xanh **0** · link «Mở /crm» **0** · chữ «BẢN THIẾT KẾ» **0** |
| **B5** | ✅ **ĐẠT** | Hero = **2** block `ĐÃ ĐẾN` / `CHƯA ĐẾN`, nút ◯/✕ = **0**, số cộng đúng (1+351=352 · 1+155=156), hai block chính là nút lọc |
| **C1b** | ✅ **ĐẠT** (một điểm không kiểm được trên desktop) | «Chụp ảnh» mở đúng `#cam` (`capture="environment"`), «Thêm hình từ thiết bị» mở `#pick` (không capture) — **hai đường tách bạch**. C2: **6/6** ca (2 nút × 201/500/rớt mạng) **không** xê dịch điểm danh |
| **D0** | ✅ **ĐẠT** | **4 màn** một cỡ duy nhất: `/crm` `{48x48:179}` · classic `{48x48:179}` · cửa Gala `{46x46:176}` · cửa Tọa đàm `{46x46:93}` — `object-fit` chỉ `cover` |
| **D2** | ✅ **ĐẠT** | `.pf-av` **đã có** `onload="imgFit(this)"` → ảnh ratio 1.362 ra **260×190 `contain`** ở crm+classic; ở cửa **250×190 `contain`** |
| **E** | ❌ **KHÔNG ĐẠT** (1 mục) | E3 ✅ 63/177/176 non-null · **0** giá trị latin · **0** tên VN bị mất · E4 ✅ xếp chồng + đảo đúng trên **4 màn, cả list lẫn hồ sơ** · lọc «Khách Nhật» ✅ **63/44 đúng bằng số khách có `name_jp`** · **search JP ở 2 cửa = 0 kết quả** ❌ |
| **Regress** | ✅ **ĐẠT** | `npm run smoke:crm` **PASS 43/43** · `?session=` lạ → **400** (4/4) · `session=gala` → 200 |

**Nợ cũ đo lại (không im PASS)**

| Nợ | Trạng thái |
|---|---|
| **CỬA-1** nút «←» bị banner đè | ✅ **đã vá** — quét 144 điểm: **132 chạm được**; chạm ngón tay thật → hồ sơ **đóng** |
| **CỬA-3** banner sticky nuốt đầu màn | ✅ **hết** — banner đã gỡ; cuộn 6 000 px rồi, `日本語` và `📅 Lịch trình` **vẫn trong màn và chạm được** |
| **CỬA-4** số không tự làm mới | ❌ **còn nguyên** — 30 s đứng yên: `/crm/guests` **2 → 2** request, `setInterval` **không có** trong trang |
| **m3** thông báo lỗi tiếng Anh | ❌ còn — máy chủ trả `upload error`, màn hiện nguyên văn |
| **m4** ảnh > 12 MiB | ❌ còn — **HTTP 500** kèm **thân HTML tiếng Anh** `Internal Server Error` |
| **m5** chặn HEIC | ❌ còn lệch — chỉ 2 trang cửa có (2 chỗ/file); `/crm` + classic **0**, mà server `ALLOWED` vẫn nhận `image/heic` |
| **i5** ảnh của khách đã xoá mềm | ❌ còn — **10** ảnh thuộc khách `deleted_at IS NOT NULL`; `GET /crm/photos/1` → **302** (vẫn tải được) |

---

## 0. CR-25 — prod chạy đúng tip `811fba3`, byte-for-byte

QC tự tải lại từ prod rồi so **sha256** với `git show 811fba3:<file>`:

| Đường dẫn prod | sha256 (16 ký tự đầu) | Blob tip | Khớp |
|---|---|---|---|
| `GET /crm` (`CRM_UI=new`) | `e60daab6db155db6` | `server/crm/views/crm-app-v2.html` | ✅ 78 551 B |
| `GET /crm/classic` | `7bdbb727d62c10e4` | `server/crm/views/crm-app.html` | ✅ 65 456 B |
| `GET /checkin-gala.html` | `6d9c8c5dd6dda74b` | `checkin-gala.html` | ✅ 30 632 B |
| `GET /checkin-toadam.html` | `ed1885e9f3097efb` | `checkin-toadam.html` | ✅ 30 585 B |

`HEAD = 811fba3` (20:19:41) · deploy `SUCCESS 2026-08-04T13:21:29Z` = 20:21:29 (+07) · `git status`: chỉ còn file báo cáo, **0 thay đổi mã ngoài git**. Lỗi «prod chạy mã chưa nằm trong git» mà vòng trước bắt được **không tái diễn**.

---

## A — số liệu

Đo 20:33, `limit=1000` (tổng 378 nên không bị cắt):

| Buổi | tab (`du_*`) | `?session=` | KPI `/crm/stats` | Lệch |
|---|---|---|---|---|
| **Gala** | 352 | 352 | 352 | **0** |
| **Tọa đàm** | 156 | 156 | 156 | **0** |

`invited 378` · `checkedIn 1` · `integrity.disjoint = true`. Regress D020: `session=` `xxx` · `gala-vip` · `.*` · `gala|toa-dam` → **400** cả 4; `session=gala` → 200.

---

## B0 — hai lối sang cửa, hai làn, ba bề ngang

| Viewport | `/crm` | `/crm/classic` | 2 link trong màn |
|---|---|---|---|
| 390px | `scrollW 390 = clientW 390` ✅ | ✅ | ✅ / ✅ |
| 360px | `360 = 360` ✅ | ✅ | ✅ / ✅ |
| 320px | `320 = 320` ✅ | ✅ | ✅ / ✅ |

Cả hai làn: `href=/checkin-toadam.html` · `/checkin-gala.html`, `target=_blank`, `rel=noopener`.

---

## B2 — `/crm` + `/crm/classic` không còn nút điểm danh với **bất kỳ** role

| Ca (khách id 305 chưa đến / id 248 đã đến) | roleTag | Nút điểm danh | Khối kết quả | Dòng chỉ đường |
|---|---|---|---|---|
| staff thật · khách **chưa** đến | STAFF | **0** ✅ | 0 | 1 — «Điểm danh tại cửa — mở «Cửa Tọa đàm» hoặc «Cửa Gala» ở đầu màn» |
| **btl** (mô phỏng `/crm/me`) · khách chưa đến | BTL | **0** ✅ | 0 | 1 |
| staff · khách **đã** đến | STAFF | **0** ✅ | **1** — «◯ Đã điểm danh · …@esuhai.local · 18:50:53 4/8/2026» | 0 |

Kết quả giống hệt trên **cả hai làn**. Đúng chốt 20:07: một đường điểm danh duy nhất ở 2 trang cửa; `/crm` chỉ còn **thông tin**.

---

## B4 · B5 — mặt cửa

| Kiểm | `/checkin-gala.html` | `/checkin-toadam.html` |
|---|---|---|
| Banner xanh (`rgb(15,149,90)`) | **0** | **0** |
| Link «Mở /crm» · chữ «Mở /crm» | **0** · không | **0** · không |
| Phần tử `#bnTot` (code mồ côi) | không còn | không còn |
| Hero: số block `.big` | **2** — `ĐÃ ĐẾN` · `CHƯA ĐẾN` | **2** |
| Nút ◯/✕ trên hero (`.fb`) | **0** | **0** |
| Số | `1` + `351` = **352** ✅ | `1` + `155` = **156** ✅ |
| Hai block có phải nút lọc | có (`data-f=came` / `no`) | có |

---

## C1b · C2 — hai nút ảnh

**Hai nút mở hai input khác nhau** (bắt ngay tại hộp chọn file, đọc `id` + thuộc tính của input đã mở):

| Bấm nút | Input mở ra | `capture` |
|---|---|---|
| «📷 Chụp ảnh khách» | **`#cam`** | **`environment`** |
| «📁 Thêm hình từ thiết bị» | **`#pick`** | *(không có)* |

**C2 — 6/6 ca, điểm danh không xê dịch** (khách 305 được đặt ở trạng thái «đã đến»):

| Nút | Máy chủ trả | Toast | Ảnh trên hồ sơ | Điểm danh | ĐÃ ĐẾN / CHƯA ĐẾN |
|---|---|---|---|---|---|
| Chụp | 201 | «Đã lưu ảnh ✓» | đổi sang url trả về | giữ | 2→2 / 350→350 |
| Chụp | 500 | «upload error» | không đổi | giữ | 2→2 / 350→350 |
| Chụp | rớt mạng | «Lỗi mạng — ảnh chưa lưu. **Điểm danh vẫn giữ.**» | không đổi | giữ | 2→2 / 350→350 |
| Thêm từ thiết bị | 201 | «Đã lưu ảnh ✓» | đổi sang url trả về | giữ | 2→2 / 350→350 |
| Thêm từ thiết bị | 500 | «upload error» | không đổi | giữ | 2→2 / 350→350 |
| Thêm từ thiết bị | rớt mạng | «Lỗi mạng — ảnh chưa lưu. Điểm danh vẫn giữ.» | không đổi | giữ | 2→2 / 350→350 |

Mỗi ca đúng **1** request `POST /crm/guests/305/photos`, `multipart/form-data`, có phần `name="photo"`. **0** lỗi JS.

**Điểm không kiểm chứng được ở đây (nói thẳng):** «Chụp = camera thật» dựa vào thuộc tính `capture="environment"` của input file — đây là cơ chế mở thẳng camera **trên trình duyệt điện thoại**; **không** dùng `getUserMedia`. Chromium desktop (kể cả khi giả lập điện thoại) **không** hiện thực `capture` (`'capture' in input` = `false`), nên trên máy bàn cả hai nút đều ra hộp chọn file và **không phân biệt được bằng máy**. Cái QC chứng minh được — và là rủi ro thật (hai nút cùng trỏ một input) — thì đã chứng minh: **hai input tách bạch, đúng input có `capture`**. Đề nghị: mở `/checkin-gala.html` trên một điện thoại thật ở tổng duyệt, bấm «Chụp ảnh khách» → phải ra **camera**, không ra thư viện. 20 giây.

---

## D0 · D2 — khung ảnh

| Màn | Số khung đo | Các cỡ đo được | `object-fit` | Ảnh được gắn class `landscape` |
|---|---|---|---|---|
| `/crm` | 179 | **`{48x48: 179}`** | `cover` | 10 (không nới) |
| `/crm/classic` | 179 | **`{48x48: 179}`** | `cover` | 14 (không nới) |
| `/checkin-gala.html` | 176 | **`{46x46: 176}`** | `cover` | 33 (không nới) |
| `/checkin-toadam.html` | 93 | **`{46x46: 93}`** | `cover` | 32 (không nới) |

**D0b đã vá**: vòng trước 2 trang cửa còn `{46, 78}`; nay một cỡ duy nhất trên cả 4 màn, `imgFit` vẫn gắn class nhưng CSS list không phản ứng.

**D2** — mở đúng khách ảnh khổ ngang (id 305, 2048×1504, ratio **1.362**):

| Chỗ | `/crm` | `/crm/classic` | 2 trang cửa |
|---|---|---|---|
| `.pf-av img` có `onload` | **có** ✅ | **có** ✅ | có |
| Khung ảnh hồ sơ | **260×190** | **260×190** | **250×190** |
| `object-fit` | **contain** | **contain** | **contain** |
| class `landscape` (img/box) | true/true | true/true | true/true |
| Thumb | 124×70 `contain` | 124×70 `contain` | — |

**Đ-D2 đã vá** — vòng trước hai màn này còn 190×190 `cover`. Ảnh dọc (ratio 0.75) vẫn giữ 190×190 `cover`, đúng.

---

## E — tiếng Nhật

### E3 · backfill (đã GO, `--commit`)

SQL độc lập, `READ ONLY`:

```
378 khách active | name_jp 63 | title_jp 177 | org_jp 176
giá trị JP chỉ có chữ latin : 0 / 0 / 0      ← luật «chỉ ô kanji» giữ nguyên
giá trị JP rỗng             : 0
khách mất tên tiếng Việt    : 0              ← COALESCE không đè field VN
audit: import_vnjb ×1 · 2026-08-04T13:20:54Z
```

Khớp con số R1 báo (63/177/176). Cả **63** khách có `name_jp` đều có `full_name` viết latin — tức bộ lọc «Khách Nhật» cũ (chỉ xét kanji trong tên VN) **bỏ sót đúng 63/63**; nay đã sửa (xem dưới).

### E4 · xếp chồng + đảo thứ tự — dữ liệu **thật** sau backfill

Khách id 367 (`Chiba Satoshi` / `千葉 聡 様`, đơn vị + chức vụ đều có cặp):

| Màn | UI **vn** (trên → dưới) | UI **jp** (trên → dưới) | Xuống dòng thật? | Bị cắt? |
|---|---|---|---|---|
| `/crm` thẻ danh sách | `Chiba Satoshi` → `千葉 聡 様` | `千葉 聡 様` → `Chiba Satoshi` | ✅ | không — **302/302 px** |
| `/crm` hồ sơ | như trên | như trên | ✅ | không — **398/398 px** |
| `/crm/classic` thẻ + hồ sơ | như trên | như trên | ✅ | không |
| 2 trang cửa — **thẻ danh sách** | `Chiba Satoshi` → `千葉 聡 様` | (đảo) | ✅ | không |
| 2 trang cửa — hồ sơ | tên · chức vụ · đơn vị đều có cặp | (đảo) | ✅ | không |

**E4b đã vá**: vòng trước hàng JP trên `.n1` bị ép cùng hàng rồi co về **0 px** ở 390px; nay hiện đủ. Danh sách 2 trang cửa vòng trước **không** có cặp; nay có.

### E · lọc «🎌 Khách Nhật»

| Trang cửa | Kết quả lọc | Số khách có `name_jp` trong danh sách đó | Khớp |
|---|---|---|---|
| Gala | **63** | 63 | ✅ |
| Tọa đàm | **44** | 44 | ✅ |

### ❌ E-1 · ô tìm trên 2 trang cửa không tra được chữ Nhật

Cùng bốn khoá, gõ vào ô tìm của từng màn, đếm thẻ hiện ra:

| Khoá | ý nghĩa | `/checkin-gala.html` | `/crm` |
|---|---|---|---|
| `千葉` | tên Nhật (`name_jp`) | **0** ❌ | **2** ✅ |
| `Chiba` | tên latin (`full_name`) | 2 ✅ | 2 ✅ |
| `鶴雅` | đơn vị Nhật (`org_jp`) | **0** ❌ | **1** ✅ |
| `取締役` | chức vụ Nhật (`title_jp`) | **0** ❌ | **13** ✅ |

Nguyên nhân, đọc trên đúng byte prod:

```js
// checkin-gala.html:146 và checkin-toadam.html:146
if(st.q){var h=norm(g.name+" "+g.title+" "+g.org); … }      // KHÔNG có name_jp/title_jp/org_jp
```

trong khi `crm-app-v2.html:511` và `crm-app.html:363` đã có đủ 3 field. `mapRow()` **đã** mang `name_jp/title_jp/org_jp` vào `G`, nên vá chỉ là nối thêm 3 field vào chuỗi `h` — **1 dòng × 2 file**.

Vì sao chặn PASS: phiếu wave ghi «ô tìm **4 màn**», phiếu QC này ghi «search ra tên JP», và commit `00ee6b0` khai đã làm cả 4. Đây là chỗ **PG Nhật đứng ở cửa** — có dữ liệu JP mà tra không ra thì phần lớn giá trị của E3 nằm lại ở `/crm`, không tới được cửa. Đường vòng còn dùng được: tra bằng tên latin, hoặc bấm «🎌 Khách Nhật» rồi cuộn 63 thẻ.

---

## Nợ cũ — đo lại từng cái

### ✅ CỬA-1 · nút «←» đóng hồ sơ

```
hộp nút: x=14 y=12 38×38
quét 144 điểm khắp hộp → 132 điểm chạm trúng chính nút (12 điểm rìa rơi vào .pf-bar)
chạm ngón tay thật đúng tâm → #pf mất class .show  ⇒ hồ sơ ĐÓNG
```

Vòng trước: **0/648** điểm chạm được, chạm xong hồ sơ vẫn mở. **Đã vá thật.**

### ✅ CỬA-3 · đầu màn sau khi cuộn

Cuộn xuống **6 000 px** (cửa Gala) / **3 479 px** (cửa Tọa đàm) rồi đo: `#langBtn` và `#lichBtn` **đều trong màn** và `elementFromPoint` trả về **chính nút** (không bị phần tử nào đè). Banner đã gỡ nên nguồn gây lỗi không còn.

### ❌ CỬA-4 · số ĐÃ ĐẾN / CHƯA ĐẾN không tự làm mới

| Đo | Cửa Gala | Cửa Tọa đàm |
|---|---|---|
| Request `/crm/guests` lúc tải xong | 2 | 2 |
| Sau **30 giây** đứng yên | **2** | **2** |
| `setInterval` trong trang | **không có** | **không có** |

Số hero là **ảnh chụp lúc tải trang** của riêng máy đó. Hệ quả ở cửa: hai PG hai điện thoại cùng cửa → mỗi máy chỉ cộng những ca **chính nó** vừa điểm danh; thẻ khách bên máy kia vẫn hiện «chưa đến». Không hỏng dữ liệu (POST trùng trả `already`), nhưng con số Sponsor muốn nhìn sẽ **lệch giữa các máy** suốt buổi, và PG có thể chạy lại thao tác đã có người làm. Chưa có cơ chế nào (polling nhẹ 15–30 s, hoặc nút «tải lại số») — QC không tự chọn, nêu để anh quyết.

### ❌ Bốn lỗi nhỏ nhóm C còn nguyên

| Mã | Đo được hôm nay |
|---|---|
| **m3** | Máy chủ trả `{"error":"upload error"}` → màn cửa hiện **nguyên văn tiếng Anh** «upload error» |
| **m4** | Gửi ảnh **13 MiB** (tới id khách không tồn tại, nên không ghi gì): **HTTP 500**, thân là **HTML tiếng Anh** `Internal Server Error`. Trang cửa parse JSON thất bại nên rơi về «Không lưu được ảnh» — người dùng không biết vì sao. Ảnh 48 MP của điện thoại đời mới có thể vượt 12 MiB |
| **m5** | Chặn HEIC chỉ có ở **2 trang cửa** (2 chỗ/file); `/crm` và classic **0**; server `ALLOWED` vẫn nhận `image/heic` → tải HEIC từ `/crm` thì thẻ có ảnh mà màn không hiện được |
| **i5** | **10** ảnh thuộc khách đã xoá mềm; `GET /crm/photos/1` (khách `deleted_at IS NOT NULL`) → **302** presigned MinIO, tải được bình thường |

---

## Regress

`npm run smoke:crm` (đầy đủ, có `DATABASE_URL` + phần UI Playwright): **PASS 43/43**, gồm `AC-4 /crm/stats khớp SQL độc lập ±0`, `AC-5 số thẻ UI khớp invited — UI 378 vs API 378`, `AC-5 không lỗi JS — sạch`. `?session=` giá trị lạ → 400 (4/4). **0** lỗi JS trên cả 4 màn trong toàn bộ lượt đo.

---

## Việc phải làm để lật FAIL → PASS

1. **E-1** — nối `name_jp`/`title_jp`/`org_jp` vào chuỗi tìm ở `checkin-gala.html:146` và `checkin-toadam.html:146` (1 dòng × 2 file), rồi đo lại đúng bảng 4 khoá ở trên.
2. **CỬA-4** — chốt cách làm mới số (polling nhẹ / nút tải lại) rồi làm; hoặc anh chấp nhận rủi ro và ghi vào runbook cửa: **mỗi cửa một máy đếm**, các máy khác chỉ tra.

Bốn lỗi nhỏ nhóm C (m3 · m4 · m5 · i5) không chặn PASS nhưng đã ba vòng chưa đụng tới — đề nghị gom một vé sau lễ.

---

## Cách đo lại

- Prod, làn Bearer smoke (role `staff`); token chỉ trong env `chmod 600`, xoá sau khi chạy.
- SQL qua `DATABASE_PUBLIC_URL`, mọi truy vấn trong `BEGIN TRANSACTION READ ONLY`.
- UI: Playwright Chromium, chặn mọi request rời hạ tầng mình; đo bằng `getBoundingClientRect` · `getComputedStyle` · `elementFromPoint` · `naturalWidth/Height`; chạm bằng `touchscreen.tap`.
- Mô phỏng ở trình duyệt (role `btl`, khách đã điểm danh, 201/500/rớt mạng khi tải ảnh) bằng `context.route()`; **không** request nào ghi vào prod.
- Ca thử >12 MiB gửi tới `/crm/guests/999999999/photos` — `photos.js` kiểm khách tồn tại **trước** khi ghi MinIO/Postgres, nên không tạo rác.
