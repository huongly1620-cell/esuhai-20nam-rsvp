# Báo cáo R1 — E08-D028 gói còn lại (E-1 · CỬA-4 · F · G)

Tip cuối **`2a429ab`** · nền `dbe7f7d` (F0) → `28c53ce` (E-1) → `2a429ab` (CỬA-4 · F · G)
CR-25: `railway up --service esuhai-web` sau khi xác nhận 3 service · byte prod ↔ HEAD **khớp**
Smoke: `PASS — 43/43`

---

## Cắt 1 — E-1 (lật blocker Gate 2)

`match()` chỉ ghép `name+title+org`, thiếu ba field JP mà `mapRow` đã mang sẵn từ E3.
1 dòng × 2 file. Đo trên trình duyệt thật ở prod:

| khoá | Gala | Tọa đàm |
|---|---|---|
| 千葉 | **2** (trước 0) | **2** |
| 鶴雅 | **1** | **1** |
| 取締役 | **13** | **9** |
| Chiba (latin) | 2 — không regress | 2 |

`norm()` dùng NFD rồi bỏ dấu tổ hợp U+0300–U+036F; dakuten (U+3099) nằm ngoài khoảng đó nên
chữ Nhật đi qua nguyên vẹn, và hai vế so khớp cùng một phép chuẩn hoá.

## Cắt 2 — CỬA-4 (P1 + P2)

Poll 25s · `visibilitychange` · nút ↻ · dòng «Cập nhật HH:MM». Không websocket.

Bốn chốt chặn, mỗi cái ứng với một cách hỏng cụ thể ở cửa:

| chặn | nếu không có |
|---|---|
| không poll khi hồ sơ đang mở | list nhảy dưới tay PG → điểm danh nhầm người |
| `mutSeq` huỷ vòng poll bay ngang lúc điểm danh | dữ liệu chụp TRƯỚC lần ghi đắp đè, xoá dấu vừa điểm danh |
| chỉ vẽ lại khi chữ ký (id · đã đến · **ảnh** · bàn) đổi | đập `innerHTML` mỗi 25s dù chẳng có gì mới |
| `gate()` dựng cờ `DEAD` | trang đã bị thay ruột vẫn gõ máy chủ mỗi 25s, lỗi bị nuốt im |

`merge()` giữ `_came` **và** `_by`, và **chỉ bật lên, không hạ xuống**: cửa không có route huỷ
điểm danh, nên thẻ tự lật ngược «đã đến» → «chưa đến» chỉ có thể là đọc trượt.

## Cắt 3 — AC-F1…F7 + F6 dry-run

**Luật map tag tách sang `server/crm/vnjb-keys.js`, dùng chung với script backfill.**
`import-vnjb.js` là IIFE chạy ngay khi `require` — script khác không mượn hàm của nó được, mà
chép lại luật chuẩn hoá chính là lỗi đã làm 4 thẻ khách không ai tìm ra ở D026.

| cột 4 | tag | ghi chú |
|---|---|---|
| VIP | `vip` | đã có |
| Gia đình - Nội | `Nội` | |
| Gia đình - Ngoại | `Ngoại` | |
| Gia đình TGĐ | `Gia đình TGĐ` | tag **riêng**, không gộp Nội |
| OB Esuhai | `pl:ob` | cùng chuẩn với cột 2 |
| Thường (187) / trống (37) | *(không tag)* | không nút lọc nào dùng |

F2/F5/F7: nhãn `VIP` viết hoa ở **cả 4 màn**, badge trên list, pill **HẠNG** trong hồ sơ,
thêm nút lọc «Gia đình TGĐ». Lọc Khách Nhật **giữ** `isKanji(full_name)` theo AC-F4.

### F6 dry-run — CHƯA `--commit`

```
dòng khách SoT 332 (224 dòng không sinh tag)
dò thẻ: khớp ext 62 · khớp TÊN duy nhất 46 · trùng tên 0 · không thấy 0
thẻ liên quan 108 · thẻ SẼ ĐỔI 42

  tag            thêm mới   đã có
  Gia đình TGĐ         5        0
  Ngoại               17        0
  Nội                 16        0
  pl:ob                3       19   → đủ 22
  vip                  1       47   → đủ 48
```

Khớp từng số với bảng đếm SoT. Backfill **merge**, đọc-ghi trong cùng transaction có
`FOR UPDATE`; không đụng tag buổi; chỉ thẻ `deleted_at IS NULL`.

> **Ba nút lọc Nội / Ngoại / Gia đình TGĐ hiện ra danh sách RỖNG** cho tới khi anh GO
> `--commit`. Ở cửa, nút bấm ra rỗng trông y như nút hỏng.

## Cắt 4 — AC-G0…G3

**G0** accent vốn đã khác (`--acc` Gala `#2E7FCC` · Tọa đàm `#0F955A`) nhưng **nền, lớp hồ sơ,
thanh tìm, ô ảnh trống thì hai cửa navy y hệt nhau**. Nay nền cũng tách (Tọa đàm xanh lá đậm),
gói trong biến `:root` nên hai file chỉ khác **đúng một dòng màu**.

**G1** thứ tự khối hồ sơ đo trên trình duyệt, khớp mock:
`pf-av · pf-nm · pf-rl · th(BÀN) · hangp(HẠNG) · facts · info · cib(CTA) · quay`

**G2** CTA giữ nguyên POST check-in đã có. **G3** hai nút ảnh chuyển xuống khối «Ghi nhận tại
quầy» đúng vị trí mock «Ảnh khách».

## Cắt 5 — AC-G4: lưu THẬT, không phải vỏ

Khảo sát API cho kết quả tốt hơn dự kiến — **không cần migration nào**:

| ô mock | lưu bằng gì | trạng thái |
|---|---|---|
| Quà khách tặng (text) | `POST /crm/guests/:id/interactions` `kind=qua-tang` | ✅ thật |
| Quà đáp lễ (引出物) | `kind=qua-dap-le` | ✅ thật |
| Ghi chú quầy | `kind=ghi-chu-quay` | ✅ thật |
| Dòng lịch sử actor + giờ | `interactions[]` của endpoint chi tiết | ✅ có sẵn, `enrich()` trước đây vứt đi |
| Ảnh quà | `kind=anh-qua` + `crm_photos.interaction_id` | ✅ thật |
| Danh sách quà đáp lễ | *(chưa có bảng nào)* | ⏳ giữ placeholder «chờ chị Thúy Hà xác nhận» |

**Hai quyết định đáng nói:**

1. **Không nhét quà vào `crm_guests.note`.** Importer ghi `note = COALESCE($4, note)` — một lượt
   import/backfill sau này sẽ **xoá sạch** mọi ghi nhận quà. Note cũng không có ai-lúc-nào nên
   không dựng được dòng lịch sử.
2. **Ảnh quà phải gắn `interaction_id`.** Avatar khách ở **cả 4 màn** là **ảnh mới nhất** của
   thẻ (`ORDER BY created_at DESC LIMIT 1`). Tải ảnh quà lên trần thì cái hộp quà thế chỗ mặt
   khách trên mọi danh sách — và **không có route xoá ảnh** để lùi lại. Cột `interaction_id` đã
   tồn tại từ đầu, 202/202 ảnh cũ đang NULL, nên thêm điều kiện `interaction_id IS NULL` vào
   truy vấn avatar **không đổi gì với dữ liệu đang có** (smoke 43/43 xác nhận).

### Đường thất bại đã đo (không ghi gì vào prod)

| tình huống | HTTP | client |
|---|---|---|
| khách không tồn tại | 404 | toast đỏ, không báo xanh giả |
| nội dung rỗng | 400 | không tạo dòng rác |
| không đăng nhập | 401 | fail-closed |

Nút «Lưu ghi nhận» kiểm `r.ok` — đúng lớp lỗi CỬA-2 (`fetch` không ném khi 4xx/5xx).

> **Đường THÀNH CÔNG chưa đo trên prod**: lưu ghi nhận thật sẽ tạo dòng vĩnh viễn trên thẻ khách
> thật và **không có route xoá interaction**. Em dừng đúng chỗ đã dừng với Đ2 — chờ anh chỉ định
> thẻ.

---

## Ba lỗi sửa kèm (đo được, không nằm trong danh sách cắt)

1. **Toast bị chôn dưới lớp hồ sơ.** `.ci-toast{z-index:99}` vs `.pf{z-index:100000}` — bản vá
   CỬA-1 nâng `.pf` lên đã vô tình chôn toast. Mọi lời báo lỗi đều bắn **từ trong** hồ sơ:
   check-in bị từ chối, ảnh không lưu được. CỬA-2 chặn được «báo xanh giả» nhưng thay bằng
   **im lặng tuyệt đối** — PG bấm, không thấy gì. Đo lại sau vá: toast chạm được ✅.
2. **Nút ↻ đặt trong hero bóp khối số.** Ở 320px khối «ĐÃ ĐẾN» còn 77px trong khi «CHƯA ĐẾN»
   giữ 165px (chuỗi «308/348» cỡ 2.7rem không co được), header 207→267px. Dời lên thanh đầu
   trang; dòng «Cập nhật …» để ngoài header sticky. Sau vá: 320/360/390 **không tràn**, hero
   360/390 cân bằng `[176,176]`.
3. **Ô «GHẾ» in ra số bàn.** `g.seat` là chuỗi «Số bàn (SoT)» của lô tgd116 — một số BÀN thứ hai,
   không phải ghế. Đo prod: **0/378 khách có đồng thời BÀN và «ghế»**. Nay gộp làm BÀN, ô GHẾ chỉ
   hiện khi có dữ liệu ghế thật (chưa có nguồn nào — spec cấm tự xếp bàn/ghế). List cũng thôi vẽ
   badge «Bàn» rỗng cho 30 thẻ không có số bàn.

## Đo lại toàn bộ trên trình duyệt thật (prod, 390px)

| | Gala | Tọa đàm |
|---|---|---|
| tràn ngang 320/360/390 | không · không · không | không · không · không |
| lỗi JS | 0 | 0 |
| accent | `#2E7FCC` | `#0F955A` |
| nút ↻ → request list mới | 1 | 1 |
| badge list (VIP · 🎌) | 44 · 63 | 30 · 44 |
| lọc ★VIP · 🎌 · Nội · Ngoại · GĐTGĐ | 44 · 63 · 0 · 0 · 0 | 30 · 44 · 0 · 0 · 0 |
| pill HẠNG | «VIP» | «VIP» |
| khối G4 đủ 8 phần tử | ✅ | ✅ |
| toast nhìn thấy khi hồ sơ mở | ✅ | ✅ |
| CỬA-1 nút ← | chạm được | chạm được |

## Số liệu prod trôi trong lúc làm

**Ly xoá mềm 30 thẻ lúc 15:56Z** (nhật ký: `guest_delete` × 30, actor `lylth`) — dọn thẻ trùng.
Khách active **378 → 348**. Vì vậy ★VIP đo lại là **44/30**, không còn **49/31** như lúc chốt F0.
Số của F0 đúng tại thời điểm đo; chênh lệch là do dữ liệu, không phải mã.

## Nợ còn treo (không nằm trong gói)

- **Smoke mù hoàn toàn với 2 trang cửa** — `grep 'checkin\|session=' tools/smoke-crm*` = 0.
  Gói này sửa gần như trọn hai file cửa mà «PASS 43/43» không nói gì về cửa. Đề xuất vé riêng.
- **Chưa từng có bằng chứng trên phiên `btl` thật** — `staff_users` = 4 btl / 0 staff; mọi kết
  luận về vai ba vòng qua đều là mô phỏng phía client.
- C1b `getUserMedia` trên điện thoại thật · m3–m5 · i5 · L2/N3 · D025 PROTECTED — HOLD sau lễ.

---

§B7: bản này do R1 (người code) viết. Gate 2 gói còn lại phải do người ≠ R1 chạy.
